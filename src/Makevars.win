# Build the native CZI bridge on Windows by default. The small native stub is
# retained as an explicit emergency fallback for systems where native-czi.cpp
# cannot be compiled.
ifeq ($(WSITOOLS_DISABLE_NATIVE_CZI),1)
OBJECTS = native-stubs.o
else
OBJECTS = native-czi.o
endif
