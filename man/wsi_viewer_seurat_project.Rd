% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat.R
\name{wsi_viewer_seurat_project}
\alias{wsi_viewer_seurat_project}
\title{Open a multi-tissue Seurat project in the wsiTools viewer}
\usage{
wsi_viewer_seurat_project(
  seurat = NULL,
  images = NULL,
  linked = NULL,
  image_names = NULL,
  labels = NULL,
  spatial_dir = NULL,
  scalefactors_json = NULL,
  tissue_positions = NULL,
  reduction = "pca",
  dims = c(1L, 2L),
  coordinate_scale = c("auto", "none", "fullres", "hires", "lowres", "seurat_image",
    "custom"),
  scale_x = NULL,
  scale_y = NULL,
  coordinate_flip = c("none", "vertical", "horizontal"),
  coordinate_rotation = c(0, 90, 180, 270),
  coordinate_transform = "none",
  spot_genes = NULL,
  default_gene = NULL,
  spot_radius = NULL,
  max_points = 100000L,
  colour_by = c("component_1", "gene", "none"),
  mode = c("tiles", "thumbnail"),
  output = NULL,
  open = interactive(),
  overwrite = FALSE,
  title = "wsiTools Seurat project viewer",
  width = 1600,
  height = NULL,
  tile_dir = NULL,
  tile_size = 512,
  tile_format = c("jpg", "png"),
  quality = 90,
  rebuild = FALSE,
  tile_overlap = NULL,
  roi_class_presets = wsi_roi_class_presets()
)
}
\arguments{
\item{seurat}{A Seurat object, Seurat-like object, or a list of one object
per image. Ignored when \code{linked} is supplied.}

\item{images}{Named character vector/list of high-resolution image paths or
\code{wsi_slide} objects. Names are used as Seurat spatial image names when
\code{image_names} is not supplied.}

\item{linked}{Optional \code{wsi_seurat_spatial} object, or named list of objects
returned by \code{\link[=wsi_link_seurat_image]{wsi_link_seurat_image()}}. Supplying \code{linked} skips the
linking step.}

\item{image_names}{Seurat spatial image names, one per image. For multiple
images, either \code{image_names} or names on \code{images} should be supplied.}

\item{labels}{Optional labels shown in the Project panel.}

\item{spatial_dir}{Optional 10x Genomics \verb{spatial/} directory. When supplied,
wsiTools looks for \code{scalefactors_json.json} and \code{tissue_positions.csv} or
\code{tissue_positions_list.csv} and uses full-resolution Space Ranger
coordinates when available.}

\item{scalefactors_json}{Optional path to a 10x Space Ranger
\code{scalefactors_json.json} file. This is useful when the Seurat object stores
only downsampled image coordinates.}

\item{tissue_positions}{Optional path to a 10x Space Ranger tissue positions
CSV file. These files contain full-resolution pixel coordinates and are the
preferred source for alignment to an external high-resolution image.}

\item{reduction}{Dimensional reduction to extract, for example \code{"pca"}.}

\item{dims}{Two reduction dimensions to plot.}

\item{coordinate_scale}{How to map Seurat image coordinates onto \code{image}.
\code{"auto"} rescales from the stored Seurat image dimensions when coordinates
appear to be in that preview space. \code{"none"} uses coordinates as supplied.
\code{"fullres"} treats coordinates as 10x full-resolution pixels and only
applies a final scale if the external image dimensions differ from the 10x
inferred full-resolution dimensions. \code{"hires"} and \code{"lowres"} convert from
10x hires/lowres preview pixels back to full-resolution pixels.
\code{"seurat_image"} always rescales from the stored Seurat image dimensions.
\code{"custom"} uses \code{scale_x} and \code{scale_y}.}

\item{scale_x, scale_y}{Custom coordinate scale factors used when
\code{coordinate_scale = "custom"}.}

\item{coordinate_flip}{Orientation flip applied after coordinate scaling and
before rotation. Choices are \code{"none"}, \code{"vertical"} (\code{y1 = -y}, displayed
as \code{image_height - y}) and \code{"horizontal"} (\code{x1 = -x}, displayed as
\code{image_width - x}). The misspelling \code{"verticcal"} is accepted as an alias.}

\item{coordinate_rotation}{Clockwise rotation applied after
\code{coordinate_flip}. Choices are \code{0}, \code{90}, \code{180}, and \code{270} degrees.}

\item{coordinate_transform}{Legacy one-step orientation transform kept for
backwards compatibility. Prefer \code{coordinate_flip} and
\code{coordinate_rotation} for new code. For example, the old
\code{"x_neg_y_y_neg_x"} transform is equivalent to
\code{coordinate_flip = "horizontal"} and \code{coordinate_rotation = 90}.}

\item{spot_genes}{Optional character vector of gene names to extract and
send to the viewer for spot colouring. Only these genes are embedded in the
viewer payload; the full expression matrix is never copied into the
browser. In live mode, \code{\link[=wsi_viewer_seurat]{wsi_viewer_seurat()}} can instead retrieve one
selected gene at a time from the active R session, so \code{spot_genes} may be
left \code{NULL}.}

\item{default_gene}{Optional gene name used as the initial spot colour
variable. The gene is automatically added to \code{spot_genes} when needed.}

\item{spot_radius}{Spot marker radius, in slide pixels. When \code{NULL}, an
estimate is taken from Seurat scale factors when available.}

\item{max_points}{Maximum number of spots to keep in the browser payload.
This protects interactive HTML viewers from very large objects.}

\item{colour_by}{Spot colour mode. \code{"component_1"} colours by the first
plotted reduction component; \code{"gene"} colours by \code{default_gene}; \code{"none"}
uses one colour.}

\item{mode}{Viewer mode. \code{"tiles"} uses prebuilt Deep Zoom tiles and is the
recommended mode for real WSI or high-resolution tissue images.
\code{"thumbnail"} is useful for quick tests and mock slides.}

\item{output, open, overwrite, title}{Viewer output options.}

\item{width, height}{Thumbnail/navigator size options passed to the viewer.}

\item{tile_dir}{Directory where per-image Deep Zoom tile pyramids are stored.}

\item{tile_size, tile_format, quality, rebuild, tile_overlap}{Deep Zoom tiling
options used in tiled mode.}

\item{roi_class_presets}{ROI classes used by the annotation UI.}
}
\value{
The HTML viewer path.
}
\description{
\code{wsi_viewer_seurat_project()} links several Seurat spatial images to their
corresponding high-resolution tissue images and opens them as one wsiTools
project. Each tissue section keeps its own spatial spot overlay, available
reduction plots, annotations, and trajectories. In tiled mode, Deep Zoom tiles
are generated for each source image with libvips so zooming can reach the full
available image resolution without loading the complete image into R memory.
}
\examples{
\dontrun{
images <- c(
  anterior1 = "V1_Mouse_Brain_Sagittal_Anterior_image.tif",
  anterior2 = "V1_Mouse_Brain_Sagittal_Anterior_Section_2_image.tif"
)

html <- wsi_viewer_seurat_project(
  brain.merge,
  images = images,
  image_names = names(images),
  output = "seurat_project.html",
  mode = "tiles"
)
}
}
