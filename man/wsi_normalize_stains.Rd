% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stain.R
\name{wsi_normalize_stains}
\alias{wsi_normalize_stains}
\alias{wsi_normalise_stains}
\title{Normalize H&E stain appearance for a patch or tile}
\usage{
wsi_normalize_stains(
  image,
  method = c("macenko", "vahadane", "fixed"),
  target = NULL,
  source_matrix = NULL,
  target_matrix = NULL,
  source_concentrations = NULL,
  target_concentrations = NULL,
  concentration_percentile = 99,
  luminosity_threshold = 0.8,
  od_threshold = 0.15,
  angular_percentile = 1,
  max_pixels = 10000,
  nmf_iterations = 80,
  sparsity = 0.05,
  epsilon = 1/255,
  format = c("array", "raster", "magick", "result")
)

wsi_normalise_stains(
  image,
  method = c("macenko", "vahadane", "fixed"),
  target = NULL,
  source_matrix = NULL,
  target_matrix = NULL,
  source_concentrations = NULL,
  target_concentrations = NULL,
  concentration_percentile = 99,
  luminosity_threshold = 0.8,
  od_threshold = 0.15,
  angular_percentile = 1,
  max_pixels = 10000,
  nmf_iterations = 80,
  sparsity = 0.05,
  epsilon = 1/255,
  format = c("array", "raster", "magick", "result")
)
}
\arguments{
\item{image}{RGB/RGBA array, raster object, or magick image.}

\item{method}{Stain-estimation method: \code{"macenko"}, \code{"vahadane"}, or
\code{"fixed"}.}

\item{target}{Optional reference image used to estimate target vectors and
target concentration percentiles.}

\item{source_matrix}{Optional source H&E stain matrix. If omitted, it is
estimated from \code{image} unless \code{method = "fixed"}.}

\item{target_matrix}{Optional target H&E stain matrix. If omitted and
\code{target} is not supplied, standard H&E vectors are used.}

\item{source_concentrations, target_concentrations}{Optional hematoxylin/eosin
concentration percentile values. When omitted, source values are measured
from \code{image}; target values come from \code{target} when supplied, otherwise
source values are reused so only stain-vector normalisation is applied.}

\item{concentration_percentile}{Percentile used for concentration scaling.}

\item{luminosity_threshold}{Pixels brighter than this RGB mean are treated as
background during estimation.}

\item{od_threshold}{Minimum optical-density sum for tissue-like pixels.}

\item{angular_percentile}{Macenko angular percentile.}

\item{max_pixels}{Maximum number of tissue-like pixels used for estimation.}

\item{nmf_iterations, sparsity}{Controls for the experimental
Vahadane-style NMF estimator.}

\item{epsilon}{Lower bound used before taking optical-density logarithms.}

\item{format}{\code{"array"}, \code{"raster"}, \code{"magick"}, or \code{"result"} for a detailed
object containing the normalized image, matrices, and concentration
summaries.}
}
\value{
A normalized image or a \code{wsi_stain_normalization} object.
}
\description{
Performs dependency-free H&E stain normalisation on an already-small image
patch, tile, or thumbnail. The default \code{method = "macenko"} estimates source
H&E stain vectors from the supplied image and reconstructs the patch with
target H&E vectors. \code{method = "vahadane"} provides a lightweight
Vahadane-style non-negative matrix-factorisation estimate. \code{method = "fixed"}
uses the standard H&E vectors without estimation.
}
\details{
This function is deliberately patch-oriented. For whole-slide images, call
\code{\link[=wsi_stain_normalize_region]{wsi_stain_normalize_region()}} or apply it inside a tiled workflow; do not
load an entire WSI into R memory.
}
\examples{
patch <- array(0.8, dim = c(32, 32, 3))
normalized <- wsi_normalize_stains(patch, method = "fixed")
}
