% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat.R
\name{wsi_viewer_seurat}
\alias{wsi_viewer_seurat}
\title{Open a Seurat spatial object in the wsiTools viewer}
\usage{
wsi_viewer_seurat(
  seurat,
  image,
  linked = NULL,
  live = TRUE,
  dynamic_tiles = live,
  show_spots = TRUE,
  mode = c("tiles", "thumbnail"),
  output = NULL,
  open = interactive(),
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{seurat}{A Seurat object.}

\item{image}{Path to the high-resolution image, or a \code{wsi_slide} object.}

\item{linked}{Optional precomputed object from \code{\link[=wsi_link_seurat_image]{wsi_link_seurat_image()}}.}

\item{live}{Use \code{\link[=wsi_viewer_live]{wsi_viewer_live()}} instead of static \code{\link[=wsi_viewer]{wsi_viewer()}}. The
default is \code{TRUE} so R and the browser stay synchronized while the session
is active; set \code{live = FALSE} to write a static HTML viewer.}

\item{dynamic_tiles}{When \code{live = TRUE}, serve the image as dynamic tiles.}

\item{show_spots}{Whether to draw the spatial spot layer on top of the
image.}

\item{mode}{Viewer mode for static output. \code{"tiles"} gives full-resolution
Deep Zoom viewing when the backend can create tiles.}

\item{output, open, overwrite}{Additional viewer options.}

\item{...}{Arguments passed to \code{\link[=wsi_link_seurat_image]{wsi_link_seurat_image()}} and then to
\code{\link[=wsi_viewer]{wsi_viewer()}} or \code{\link[=wsi_viewer_live]{wsi_viewer_live()}}. Viewer arguments take precedence
after the linked object is created.}
}
\value{
A \code{wsi_viewer_session} by default. If \code{live = FALSE}, returns the
static HTML path.
}
\description{
\code{wsi_viewer_seurat()} opens a high-resolution tissue image and overlays
Seurat spatial spots. The top \strong{Seurat} menu opens buttons for the
dimensional reductions found in the object, and can show/hide or zoom to the
spot overlay. In live mode, typing a gene in the Seurat menu retrieves only
that selected gene from R; static HTML viewers use genes embedded with
\code{spot_genes}.
}
\examples{
\dontrun{
viewer <- wsi_viewer_seurat(
  brain,
  "/Users/stefano/Downloads/V1_Mouse_Brain_Sagittal_Anterior_image.tif",
  mode = "tiles"
)
}
}
