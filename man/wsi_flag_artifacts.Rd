% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifact-filter.R
\name{wsi_flag_artifacts}
\alias{wsi_flag_artifacts}
\alias{wsi_filter_artifact_tiles}
\title{Flag or filter artifact tiles in a grid}
\usage{
wsi_flag_artifacts(
  slide,
  grid,
  options = wsi_artifact_options(),
  action = c("flag", "drop")
)

wsi_filter_artifact_tiles(
  slide,
  grid,
  options = wsi_artifact_options(),
  action = c("drop", "flag")
)
}
\arguments{
\item{slide}{A \code{wsi_slide} object.}

\item{grid}{Tile grid from \code{\link[=wsi_tile_grid]{wsi_tile_grid()}} or \code{\link[=extract_tiles]{extract_tiles()}}.}

\item{options}{Thresholds from \code{\link[=wsi_artifact_options]{wsi_artifact_options()}}.}

\item{action}{\code{"flag"} keeps all rows with artifact columns; \code{"drop"} removes
rows where \code{artifact_flag} is \code{TRUE}.}
}
\value{
A tile grid or manifest with artifact metrics.
}
\description{
Reads only the regions listed in \code{grid}, computes artifact metrics for each
tile, appends artifact columns, and optionally drops flagged tiles. This is a
tile-by-tile quality-control step; it never loads an entire whole-slide image
into R memory.
}
