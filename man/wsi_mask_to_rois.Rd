% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask-annotations.R
\name{wsi_mask_to_rois}
\alias{wsi_mask_to_rois}
\alias{wsi_read_mask_annotations}
\alias{mask_to_rois}
\alias{read_mask_annotations}
\title{Convert an annotation mask into ROI polygons}
\usage{
wsi_mask_to_rois(
  mask,
  channel = c("auto", "gray", "red", "green", "blue", "alpha", "rgb"),
  threshold = NULL,
  background = 0,
  label_values = NULL,
  class_map = NULL,
  color_map = NULL,
  origin = c(x = 0, y = 0),
  scale = c(x = 1, y = 1),
  connectivity = c("4", "8"),
  min_area = 1,
  simplify = TRUE,
  prefix = "mask"
)

wsi_read_mask_annotations(path, ...)

mask_to_rois(
  mask,
  channel = c("auto", "gray", "red", "green", "blue", "alpha", "rgb"),
  threshold = NULL,
  background = 0,
  label_values = NULL,
  class_map = NULL,
  color_map = NULL,
  origin = c(x = 0, y = 0),
  scale = c(x = 1, y = 1),
  connectivity = c("4", "8"),
  min_area = 1,
  simplify = TRUE,
  prefix = "mask"
)

read_mask_annotations(path, ...)
}
\arguments{
\item{mask}{A matrix, logical/numeric labelled mask, raster, RGB/RGBA array,
\code{magick} image, \code{wsi_tissue_mask}, or image file path.}

\item{channel}{Image channel used when \code{mask} is an image. \code{"gray"} uses the
mean RGB value, \code{"rgb"} treats RGB colours as distinct integer labels, and
\code{"alpha"} uses transparency.}

\item{threshold}{Optional threshold. When supplied, values greater than the
threshold are treated as a single foreground label.}

\item{background}{Background label value(s) to ignore.}

\item{label_values}{Optional explicit mask label values to import.}

\item{class_map}{Optional named character vector or list mapping mask values
to pathology classes.}

\item{color_map}{Optional named colour vector/list mapping mask values to
annotation colours.}

\item{origin}{Numeric x/y origin of the mask in level-0 slide coordinates.}

\item{scale}{Numeric x/y pixel size, in slide pixels, for one mask pixel.}

\item{connectivity}{Pixel connectivity for connected components, \code{"4"} or
\code{"8"}.}

\item{min_area}{Minimum component area in mask pixels.}

\item{simplify}{Remove collinear vertices from the pixel-boundary polygon.}

\item{prefix}{Prefix for generated ROI ids and names.}

\item{path}{Image mask file path.}

\item{...}{Additional arguments passed to \code{\link[=wsi_mask_to_rois]{wsi_mask_to_rois()}}.}
}
\value{
A \code{wsi_roi} object that can be passed to \code{\link[=wsi_viewer]{wsi_viewer()}},
\code{viewer$add_rois()}, \code{\link[=write_geojson]{write_geojson()}}, \code{\link[=extract_tiles]{extract_tiles()}}, and measurement
helpers.
}
\description{
Converts a binary or labelled annotation mask into a \code{wsi_roi} object. This
is intended for already-small annotation or segmentation masks, not for
loading a full whole-slide image into memory. File paths are read with the
optional \code{magick} package; matrix, raster, array, and \code{wsi_tissue_mask}
inputs do not require a WSI backend.
}
\details{
Each connected non-background component becomes one polygon ROI. Mask pixels
are interpreted as pixel cells, so pixel \verb{(row = 1, col = 1)} spans
\code{origin} to \code{origin + scale}.
}
