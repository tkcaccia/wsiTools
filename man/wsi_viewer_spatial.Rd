% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-objects.R
\name{wsi_viewer_spatial}
\alias{wsi_viewer_spatial}
\title{Open a spatial omics object in the wsiTools viewer}
\usage{
wsi_viewer_spatial(
  object,
  image,
  linked = NULL,
  object_type = c("auto", "seurat", "giotto", "spatialexperiment"),
  live = TRUE,
  dynamic_tiles = FALSE,
  mode = c("tiles", "thumbnail"),
  output = NULL,
  open = interactive(),
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{object}{A Seurat, Giotto, SpatialExperiment, or compatible object.}

\item{image}{Path to the high-resolution image, or a \code{wsi_slide} object.}

\item{linked}{Optional linked object returned by \code{\link[=wsi_link_spatial_image]{wsi_link_spatial_image()}}
or one of the specialised linkers.}

\item{object_type}{Object family. \code{"auto"} tries to infer the backend.}

\item{live}{Use \code{\link[=wsi_viewer_live]{wsi_viewer_live()}} instead of static \code{\link[=wsi_viewer]{wsi_viewer()}}. The
default is \code{TRUE} so R and the browser stay synchronized while the session
is active; set \code{live = FALSE} to write a static HTML viewer.}

\item{dynamic_tiles}{When \code{live = TRUE}, serve the image as dynamic tiles.
The default is \code{FALSE}: live synchronization stays active, while the base
image and dense spatial masks use prebuilt Deep Zoom tiles for smoother
zooming and panning. Use \code{TRUE} only as a fallback when prebuilt tiles
cannot be created.}

\item{mode}{Viewer mode for static output. \code{"tiles"} gives full-resolution
Deep Zoom viewing when the backend can create tiles.}

\item{output, open, overwrite}{Additional viewer options.}

\item{...}{Arguments passed to the selected linker, for example
\code{coordinates}, \code{embeddings}, \code{reduction}, \code{spot_genes}, \code{default_gene},
\code{coordinate_flip}, and \code{coordinate_rotation}.}
}
\value{
A \code{wsi_viewer_session} by default. If \code{live = FALSE}, returns the
static HTML path.
}
\description{
\code{wsi_viewer_spatial()} is a common viewer entry point for Seurat, Giotto,
and SpatialExperiment-style objects. It uses live mode by default so the
R/httpuv bridge can synchronize selections and retrieve genes on demand
without serialising the full expression matrix into the browser. Set
\code{live = FALSE} to write a static HTML viewer.
}
