% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viewer.R
\name{wsi_viewer_noninteractive}
\alias{wsi_viewer_noninteractive}
\title{Create a viewer from non-interactive R scripts}
\usage{
wsi_viewer_noninteractive(
  input,
  output = NULL,
  ...,
  backend = c("auto", "openslide", "vips"),
  overwrite = FALSE,
  close_slide = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{input}{A path to an image/slide or an existing \code{wsi_slide} object.}

\item{output}{HTML output path. If \code{NULL} and \code{input} is a path, a
\verb{*_wsiTools_viewer.html} file is created in the current working directory.
If \code{input} is a slide object and \code{output = NULL}, a temporary file is used.}

\item{...}{Additional arguments passed to \code{\link[=wsi_viewer]{wsi_viewer()}}, such as \code{mode},
\code{width}, \code{stain}, \code{roi}, \code{project_images}, or \code{channel_sources}.}

\item{backend}{Backend passed to \code{\link[=wsi_open]{wsi_open()}} when \code{input} is a path.}

\item{overwrite}{Whether to overwrite \code{output} if it already exists.}

\item{close_slide}{Close the slide after writing the viewer when this
function opened it from a path.}

\item{quiet}{If \code{FALSE}, print the generated HTML path.}
}
\value{
The normalized HTML path, visibly.
}
\description{
\code{wsi_viewer_noninteractive()} is a script-friendly wrapper around
\code{\link[=wsi_open]{wsi_open()}} and \code{\link[=wsi_viewer]{wsi_viewer()}}. It never opens a browser, returns the HTML
path visibly, and prints the path unless \code{quiet = TRUE}. This is useful from
\code{Rscript}, batch jobs, Quarto/knitr setup chunks, or remote sessions where
\code{utils::browseURL()} is not available.
}
\details{
The function creates a static HTML viewer. For a live dynamic-tile viewer
from \code{Rscript}, call \code{\link[=wsi_viewer_live]{wsi_viewer_live()}} directly with \code{open = FALSE} and
\code{wait = TRUE} so the local tile/sync server remains alive.
}
\examples{
\dontrun{
html <- wsi_viewer_noninteractive(
  "sample.svs",
  output = "sample_viewer.html",
  mode = "tiles"
)

# Equivalent command-line use:
# Rscript -e 'library(wsiTools); wsi_viewer_noninteractive("sample.svs")'
}
}
