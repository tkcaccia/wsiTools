% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic-tiles.R
\name{wsi_tile_cache_dir}
\alias{wsi_tile_cache_dir}
\alias{wsi_tile_cache_info}
\alias{wsi_tile_cache_prune}
\alias{wsi_tile_cache_clear}
\title{Inspect and manage the persistent viewer tile cache}
\usage{
wsi_tile_cache_dir(create = TRUE)

wsi_tile_cache_info(cache_dir = wsi_tile_cache_dir())

wsi_tile_cache_prune(
  cache_dir = wsi_tile_cache_dir(),
  max_bytes = wsi_tile_cache_default_max_bytes(),
  max_age_days = 30
)

wsi_tile_cache_clear(cache_dir = wsi_tile_cache_dir(create = FALSE))
}
\arguments{
\item{create}{Whether to create the cache directory when it is missing.}

\item{cache_dir}{Cache directory. The default is the platform-specific R
user cache directory, or \code{WSITOOLS_DYNAMIC_TILE_CACHE_DIR} when set.}

\item{max_bytes}{Maximum cache size in bytes. The default is controlled by
\code{WSITOOLS_DYNAMIC_TILE_CACHE_GB} and otherwise uses 20 GB.}

\item{max_age_days}{Remove entries older than this many days. Use \code{Inf} to
prune only by size.}
}
\value{
\code{wsi_tile_cache_dir()} returns a path. \code{wsi_tile_cache_info()} and
\code{wsi_tile_cache_prune()} return a one-row data frame. \code{wsi_tile_cache_clear()}
returns the cache path invisibly.
}
\description{
Persistent dynamic tiles are stored outside the R session so reopening a
slide can reuse tiles generated previously. Cache entries are keyed by the
source file identity and tile settings; stale entries therefore cannot be
used for a changed image.
}
