% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmentation.R
\name{wsi_cell_segment_roi}
\alias{wsi_cell_segment_roi}
\alias{cell_segment_roi}
\title{Run optional cell segmentation on a selected ROI}
\usage{
wsi_cell_segment_roi(
  image,
  roi,
  output_dir,
  engine = c("stardist_he", "stardist_ihc", "mesmer_dapi"),
  roi_id = NULL,
  level = 0,
  crop_format = c("png", "tiff", "jpeg"),
  model = NULL,
  command = NULL,
  args = NULL,
  output = NULL,
  output_type = c("auto", "geojson", "csv", "mask"),
  prob_thresh = NULL,
  nms_thresh = NULL,
  tiles_x = NULL,
  tiles_y = NULL,
  min_area = NULL,
  nuclear_channel = "DAPI",
  membrane_channel = NULL,
  keras_home = NULL,
  pretrained_zip = NULL,
  mask_as_rois = TRUE,
  translate_geojson = TRUE,
  overwrite = FALSE,
  run = TRUE,
  backend = c("auto", "vips", "openslide"),
  ...
)

cell_segment_roi(
  image,
  roi,
  output_dir,
  engine = c("stardist_he", "stardist_ihc", "mesmer_dapi"),
  roi_id = NULL,
  level = 0,
  crop_format = c("png", "tiff", "jpeg"),
  model = NULL,
  command = NULL,
  args = NULL,
  output = NULL,
  output_type = c("auto", "geojson", "csv", "mask"),
  prob_thresh = NULL,
  nms_thresh = NULL,
  tiles_x = NULL,
  tiles_y = NULL,
  min_area = NULL,
  nuclear_channel = "DAPI",
  membrane_channel = NULL,
  keras_home = NULL,
  pretrained_zip = NULL,
  mask_as_rois = TRUE,
  translate_geojson = TRUE,
  overwrite = FALSE,
  run = TRUE,
  backend = c("auto", "vips", "openslide"),
  ...
)
}
\arguments{
\item{image}{A \code{wsi_slide} object or image path accepted by \code{\link[=wsi_open]{wsi_open()}}.}

\item{roi}{A \code{wsi_roi} object. When multiple ROIs are supplied, \code{roi_id}
selects one; otherwise the first ROI is used.}

\item{output_dir}{Directory for crops and segmentation outputs.}

\item{engine}{One of \code{"stardist_he"}, \code{"stardist_ihc"}, or
\code{"mesmer_dapi"}.}

\item{roi_id}{Optional ROI id.}

\item{level}{Pyramid level used for ROI crop export.}

\item{crop_format}{Crop image format.}

\item{model}{StarDist model name for StarDist engines.}

\item{command}{External command. StarDist engines use
\code{WSITOOLS_STARDIST_COMMAND} when this is \code{NULL}; Mesmer uses
\code{WSITOOLS_MESMER_COMMAND}.}

\item{args}{Command arguments with placeholders such as \code{\{input\}},
\code{\{output\}}, \code{\{model\}}, \code{\{tiles_x\}}, \code{\{tiles_y\}},
\code{\{min_area\}}, \code{\{nuclear_channel\}}, \code{\{membrane_channel\}},
\code{\{keras_home\}}, and \code{\{pretrained_zip\}}.}

\item{output}{Optional expected model output path.}

\item{output_type}{Output type for \code{\link[=import_segmentation]{import_segmentation()}}. Mesmer defaults
to \code{"mask"} when \code{"auto"} is supplied.}

\item{prob_thresh, nms_thresh}{Optional StarDist thresholds.}

\item{tiles_x, tiles_y}{Optional tiling values passed only through command
placeholders. Use these with a wrapper that supports tiled StarDist/Mesmer
prediction to avoid high RAM use on large ROI crops.}

\item{min_area}{Optional minimum cell area passed through \code{\{min_area\}}.}

\item{nuclear_channel, membrane_channel}{Channel names/indices passed through
command placeholders for mIHC/DAPI workflows.}

\item{keras_home, pretrained_zip}{Optional StarDist cache/model locations
passed through command placeholders.}

\item{mask_as_rois}{For mask outputs, convert connected components to cell
ROI polygons.}

\item{translate_geojson}{Whether to translate crop-local GeoJSON polygons to
slide coordinates using the selected ROI bounding box origin.}

\item{overwrite}{Whether output paths may overwrite existing files.}

\item{run}{If \code{FALSE}, return the command plan after exporting the crop.}

\item{backend}{Region export backend.}

\item{...}{Additional arguments passed to mask import when \code{output_type =
"mask"} and \code{mask_as_rois = TRUE}.}
}
\value{
A \code{wsi_cell_segmentation_result} object.
}
\description{
Exports only the selected ROI bounding box, runs an optional external
segmentation command, imports the result, and maps crop-local cells back to
slide coordinates. This is designed for memory-safe viewer workflows:
StarDist/Mesmer should process a crop or a tiled crop, never the full WSI
loaded into R memory.
}
\examples{
\dontrun{
result <- wsi_cell_segment_roi(
  slide,
  roi,
  "roi_cells",
  engine = "stardist_he",
  args = c("--input", "{input}", "--output", "{output}", "--tiles", "{tiles_y}", "{tiles_x}"),
  tiles_x = 32,
  tiles_y = 32
)
}
}
