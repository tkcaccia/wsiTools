% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{wsi_diagnose}
\alias{wsi_diagnose}
\title{Diagnose a wsiTools installation for remote debugging}
\usage{
wsi_diagnose(
  method = c("auto", "homebrew", "apt", "dnf", "winget", "conda", "manual"),
  include_optional = TRUE,
  live_test = TRUE,
  sync_test = TRUE,
  host = "127.0.0.1",
  port = 8794L,
  max_tries = 20L,
  timeout = 5
)
}
\arguments{
\item{method}{Package manager target used to build suggested backend setup
commands.}

\item{include_optional}{Whether to include optional tools in the setup plan.}

\item{live_test}{Whether to try starting a temporary \code{httpuv} server.}

\item{sync_test}{Whether to run the local browser/R sync self-test. This
requires both \code{httpuv} and \code{callr}.}

\item{host, port, max_tries}{Host, starting port, and retry count for the
temporary live-viewer self-test.}

\item{timeout}{Timeout in seconds for the local sync self-test.}
}
\value{
Invisibly returns a \code{wsi_diagnose} list.
}
\description{
\code{wsi_diagnose()} prints a read-only support report intended for GitHub
issues, remote desktops, and difficult installation sessions. It reports the
operating system, R version, package version, backend table, executable
paths, relevant environment variables, live viewer startup status, a local
browser/R synchronization self-test when possible, and suggested fixes.
}
\details{
The live synchronization self-test starts a temporary local \code{httpuv} endpoint
and, when the optional \code{callr} package is available, asks a child R process
to request that endpoint while the parent services the event loop. It does
not open a browser, install tools, or modify the system.
}
\examples{
wsi_diagnose(live_test = FALSE)
}
