% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmentation.R
\name{wsi_stardist_server}
\alias{wsi_stardist_server}
\title{Start a local ROI cell-segmentation endpoint}
\usage{
wsi_stardist_server(
  image,
  output_dir = "wsi_stardist_server",
  host = "127.0.0.1",
  port = 8787,
  path = "/segment",
  max_tries = 20L,
  engines = c("stardist_he", "stardist_ihc", "mesmer_dapi"),
  default_engine = "stardist_he",
  model = "2D_versatile_he",
  command = NULL,
  args = NULL,
  stardist_ihc_model = NULL,
  mesmer_command = NULL,
  mesmer_args = NULL,
  output_type = c("auto", "geojson", "csv", "mask"),
  prob_thresh = NULL,
  nms_thresh = NULL,
  tiles_x = NULL,
  tiles_y = NULL,
  min_area = NULL,
  nuclear_channel = "DAPI",
  membrane_channel = NULL,
  keras_home = NULL,
  pretrained_zip = NULL,
  overwrite = TRUE,
  level = 0,
  crop_format = c("png", "tiff", "jpeg"),
  backend = c("auto", "vips", "openslide"),
  cell_radius = 8,
  state = NULL,
  wait = FALSE
)
}
\arguments{
\item{image}{A \code{wsi_slide} object or slide path accepted by \code{\link[=wsi_open]{wsi_open()}}.}

\item{output_dir}{Directory for ROI crops and StarDist outputs.}

\item{host, port}{Local host and first port to try for the endpoint.}

\item{path}{URL path used for segmentation requests.}

\item{max_tries}{Number of subsequent ports to try if \code{port} is busy.}

\item{engines}{Engine presets accepted by the endpoint.}

\item{default_engine}{Engine used when the browser posts plain GeoJSON.}

\item{model, command, args, output_type, prob_thresh, nms_thresh, overwrite, level}{StarDist defaults passed to \code{\link[=wsi_cell_segment_roi]{wsi_cell_segment_roi()}}.}

\item{stardist_ihc_model}{Optional model used for \code{engine = "stardist_ihc"}.}

\item{mesmer_command, mesmer_args}{Optional Mesmer command and arguments.}

\item{tiles_x, tiles_y, min_area, nuclear_channel, membrane_channel, keras_home, pretrained_zip}{Placeholder values forwarded to external command arguments. Use tiled
StarDist/Mesmer wrappers for large selected ROIs to reduce RAM pressure.}

\item{crop_format}{Crop image format passed to \code{\link[=wsi_cell_segment_roi]{wsi_cell_segment_roi()}}.}

\item{backend}{Region export backend passed to \code{\link[=wsi_cell_segment_roi]{wsi_cell_segment_roi()}}.}

\item{cell_radius}{Radius used when returning centroid outputs as cell
GeoJSON overlays.}

\item{state}{Optional live viewer state. When supplied, the endpoint records
the selected ROI and imported segmentation directly in the R session before
returning the overlay to the browser.}

\item{wait}{If \code{TRUE}, run the httpuv event loop until interrupted.}
}
\value{
A \code{wsi_stardist_server} object with the service URL.
}
\description{
Starts an optional local HTTP endpoint for the interactive viewer's Cells
menu. The viewer posts a selected ROI GeoJSON, optionally with an engine
name. The endpoint crops that ROI, runs \code{\link[=wsi_cell_segment_roi]{wsi_cell_segment_roi()}}, converts
polygon, centroid, or mask outputs to GeoJSON cell overlays, and returns the
result and run metadata to the browser.
}
\details{
This function requires the optional \code{httpuv} package and an external
StarDist or Mesmer command/script. It is not required for installing or
loading wsiTools.
}
\examples{
\dontrun{
server <- wsi_stardist_server(
  "sample.svs",
  output_dir = "stardist_server",
  command = "python",
  args = c("run_stardist.py", "{input}", "{output}", "{model}")
)

slide <- wsi_open("sample.svs")
wsi_viewer(
  slide,
  mode = "tiles",
  segmentation_run_url = server$url
)
}
}
\keyword{internal}
