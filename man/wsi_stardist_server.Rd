% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmentation.R
\name{wsi_stardist_server}
\alias{wsi_stardist_server}
\title{Start a local StarDist ROI segmentation endpoint}
\usage{
wsi_stardist_server(
  image,
  output_dir = "wsi_stardist_server",
  host = "127.0.0.1",
  port = 8787,
  path = "/segment",
  max_tries = 20L,
  model = "2D_versatile_he",
  command = NULL,
  args = NULL,
  output_type = c("auto", "geojson", "csv", "mask"),
  prob_thresh = NULL,
  nms_thresh = NULL,
  overwrite = TRUE,
  level = 0,
  crop_format = c("png", "tiff", "jpeg"),
  backend = c("auto", "vips", "openslide"),
  cell_radius = 8,
  wait = FALSE
)
}
\arguments{
\item{image}{A \code{wsi_slide} object or slide path accepted by \code{\link[=wsi_open]{wsi_open()}}.}

\item{output_dir}{Directory for ROI crops and StarDist outputs.}

\item{host, port}{Local host and first port to try for the endpoint.}

\item{path}{URL path used for segmentation requests.}

\item{max_tries}{Number of subsequent ports to try if \code{port} is busy.}

\item{model, command, args, output_type, prob_thresh, nms_thresh, overwrite, level}{Arguments passed to \code{\link[=stardist_segment_roi]{stardist_segment_roi()}}.}

\item{crop_format}{Crop image format passed to \code{\link[=stardist_segment_roi]{stardist_segment_roi()}}.}

\item{backend}{Region export backend passed to \code{\link[=stardist_segment_roi]{stardist_segment_roi()}}.}

\item{cell_radius}{Radius used when returning centroid outputs as cell
GeoJSON overlays.}

\item{wait}{If \code{TRUE}, run the httpuv event loop until interrupted.}
}
\value{
A \code{wsi_stardist_server} object with the service URL.
}
\description{
Starts an optional local HTTP endpoint for the interactive viewer's
\verb{Start StarDist} button. The viewer posts the selected ROI GeoJSON to the
endpoint. The endpoint crops that ROI, runs \code{\link[=stardist_segment_roi]{stardist_segment_roi()}}, converts
polygon or centroid segmentation output to GeoJSON cell overlays, and returns
the result to the browser.
}
\details{
This function requires the optional \code{httpuv} package and an external StarDist
command or script. It is not required for installing or loading wsiTools.
}
\examples{
\dontrun{
server <- wsi_stardist_server(
  "sample.svs",
  output_dir = "stardist_server",
  command = "python",
  args = c("run_stardist.py", "{input}", "{output}", "{model}")
)

slide <- wsi_open("sample.svs")
wsi_viewer(
  slide,
  mode = "tiles",
  segmentation_run_url = server$url
)
}
}
