% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat.R
\name{wsi_link_seurat_image}
\alias{wsi_link_seurat_image}
\title{Link a Seurat spatial object to a high-resolution image}
\usage{
wsi_link_seurat_image(
  seurat,
  image,
  image_name = NULL,
  spatial_dir = NULL,
  scalefactors_json = NULL,
  tissue_positions = NULL,
  reduction = "pca",
  dims = c(1L, 2L),
  coordinate_scale = c("auto", "none", "fullres", "hires", "lowres", "seurat_image",
    "custom"),
  scale_x = NULL,
  scale_y = NULL,
  coordinate_transform = "none",
  spot_radius = NULL,
  max_points = 100000L,
  colour_by = c("component_1", "none")
)
}
\arguments{
\item{seurat}{A Seurat object, or a Seurat-like object with spatial
coordinates and reductions.}

\item{image}{Path to the high-resolution image, or a \code{wsi_slide} object.}

\item{image_name}{Optional Seurat spatial image name. When \code{NULL}, the first
available spatial image is used.}

\item{spatial_dir}{Optional 10x Genomics \verb{spatial/} directory. When supplied,
wsiTools looks for \code{scalefactors_json.json} and \code{tissue_positions.csv} or
\code{tissue_positions_list.csv} and uses full-resolution Space Ranger
coordinates when available.}

\item{scalefactors_json}{Optional path to a 10x Space Ranger
\code{scalefactors_json.json} file. This is useful when the Seurat object stores
only downsampled image coordinates.}

\item{tissue_positions}{Optional path to a 10x Space Ranger tissue positions
CSV file. These files contain full-resolution pixel coordinates and are the
preferred source for alignment to an external high-resolution image.}

\item{reduction}{Dimensional reduction to extract, for example \code{"pca"}.}

\item{dims}{Two reduction dimensions to plot.}

\item{coordinate_scale}{How to map Seurat image coordinates onto \code{image}.
\code{"auto"} rescales from the stored Seurat image dimensions when coordinates
appear to be in that preview space. \code{"none"} uses coordinates as supplied.
\code{"fullres"} treats coordinates as 10x full-resolution pixels and only
applies a final scale if the external image dimensions differ from the 10x
inferred full-resolution dimensions. \code{"hires"} and \code{"lowres"} convert from
10x hires/lowres preview pixels back to full-resolution pixels.
\code{"seurat_image"} always rescales from the stored Seurat image dimensions.
\code{"custom"} uses \code{scale_x} and \code{scale_y}.}

\item{scale_x, scale_y}{Custom coordinate scale factors used when
\code{coordinate_scale = "custom"}.}

\item{coordinate_transform}{Optional orientation transform applied after
coordinate scaling and before viewer display. Use \code{"x_y_y_neg_x"} when the
external image orientation requires \code{x1 = y} and \code{y1 = -x}; wsiTools adds
the required image-width offset internally, so displayed coordinates use
\code{x1 = y} and \code{y1 = image_width - x}. Aliases \code{"rotate_90_cw"} and
\code{"flip_y_rotate_90_cw"} use the same transform. The default \code{"none"}
preserves coordinates.}

\item{spot_radius}{Spot marker radius, in slide pixels. When \code{NULL}, an
estimate is taken from Seurat scale factors when available.}

\item{max_points}{Maximum number of spots to keep in the browser payload.
This protects interactive HTML viewers from very large objects.}

\item{colour_by}{Spot colour mode. \code{"component_1"} colours by the first
plotted reduction component; \code{"none"} uses one colour.}
}
\value{
A \code{wsi_seurat_spatial} object containing the slide, spot table, PCA
points, and coordinate mapping metadata.
}
\description{
\code{wsi_link_seurat_image()} extracts spatial spot coordinates and a dimensional
reduction, usually PCA, from a Seurat object and maps the spots onto a
high-resolution image. Seurat remains an optional dependency: when
\code{SeuratObject} or \code{Seurat} is installed their accessors are used; otherwise
the function falls back to Seurat-like object slots/lists.
}
\details{
The linked object can be passed to \code{\link[=wsi_viewer_seurat]{wsi_viewer_seurat()}} to open the tissue
image with a spot overlay and an interactive PCA scatter plot. The image is
opened through the usual wsiTools backends and is not loaded fully into R
memory by default.
}
\examples{
\dontrun{
library(Seurat)
library(SeuratData)
brain <- LoadData("stxBrain", type = "anterior1")
brain <- SCTransform(brain, assay = "Spatial", verbose = FALSE)
brain <- RunPCA(brain, assay = "SCT", verbose = FALSE)

linked <- wsi_link_seurat_image(
  brain,
  "/Users/stefano/Downloads/V1_Mouse_Brain_Sagittal_Anterior_image.tif"
)
}
}
