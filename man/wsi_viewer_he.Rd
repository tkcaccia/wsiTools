% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stain.R
\name{wsi_viewer_he}
\alias{wsi_viewer_he}
\title{View an H&E slide with interactive hematoxylin/eosin/residual deconvolution}
\usage{
wsi_viewer_he(
  slide,
  mode = c("tiles", "thumbnail"),
  channels = NULL,
  hematoxylin = c(0.644, 0.717, 0.267),
  eosin = c(0.093, 0.954, 0.283),
  hematoxylin_colour = "#4b3f99",
  eosin_colour = "#e85b90",
  residual_colour = "#6b7280",
  hematoxylin_strength = 1,
  eosin_strength = 1,
  residual_strength = 1,
  residual_visible = FALSE,
  ...
)
}
\arguments{
\item{slide}{A \code{wsi_slide} object.}

\item{mode}{Viewer mode passed to \code{\link[=wsi_viewer]{wsi_viewer()}}. \code{"tiles"} is recommended
for full-resolution inspection.}

\item{channels}{Optional stain channels created by \code{\link[=wsi_stain_channels]{wsi_stain_channels()}}.
If supplied, these replace the default hematoxylin and HRP/DAB channels.}

\item{hematoxylin, eosin}{RGB optical-density vectors.}

\item{hematoxylin_colour, eosin_colour, residual_colour}{Initial display
colours.}

\item{hematoxylin_strength, eosin_strength, residual_strength}{Initial display
gains.}

\item{residual_visible}{Whether residual staining is visible by default.}

\item{...}{Additional arguments passed to \code{\link[=wsi_viewer]{wsi_viewer()}}, such as \code{output},
\code{tile_dir}, \code{open}, \code{roi}, or \code{overwrite}.}
}
\value{
The HTML viewer path, invisibly.
}
\description{
Convenience wrapper around \code{\link[=wsi_viewer]{wsi_viewer()}} for H&E brightfield images. The
\code{Stains} menu exposes \code{Original}, \verb{All stains}, and show-only buttons for
hematoxylin, eosin, and residual channels. Show-only views are
auto-contrasted from visible pixels. In tiled mode, the browser recolours
only visible tiles, so the full WSI is not loaded into R memory.
}
\examples{
\dontrun{
slide <- wsi_open("he_sample.svs")
html <- wsi_viewer_he(slide, output = "he_viewer.html", open = FALSE)
wsi_close(slide)
}
}
