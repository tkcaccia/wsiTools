% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{wsi_install_native_czi}
\alias{wsi_install_native_czi}
\title{Build and configure the optional native CZI backend}
\usage{
wsi_install_native_czi(
  install = TRUE,
  ask = interactive(),
  accept_license = FALSE,
  repo = "https://github.com/ZEISS/libczi.git",
  ref = "main",
  install_dir = wsi_native_czi_default_root(),
  jobs = NULL,
  set_env = TRUE,
  persist = FALSE,
  clean = FALSE
)
}
\arguments{
\item{install}{Whether to run the download/configure/build commands. Use
\code{FALSE} to inspect the plan only.}

\item{ask}{Whether to ask before running commands or writing \verb{~/.Renviron}.}

\item{accept_license}{Set to \code{TRUE} after reviewing the libCZI license notice.}

\item{repo}{Git repository URL for ZEISS libCZI.}

\item{ref}{Git branch or tag to build.}

\item{install_dir}{Directory used for the source and build tree. Defaults to
the user wsiTools data cache.}

\item{jobs}{Number of parallel build jobs. Defaults to a conservative value.}

\item{set_env}{Whether to set \code{WSITOOLS_LIBCZIAPI} for the current R session.}

\item{persist}{Whether to write \code{WSITOOLS_LIBCZIAPI} to \verb{~/.Renviron} for
future R sessions.}

\item{clean}{Whether to remove any existing source/build tree first.}
}
\value{
A \code{wsi_native_czi_installation} object.
}
\description{
\code{wsi_install_native_czi()} is an explicit, opt-in helper that downloads and
builds ZEISS libCZI/libCZIAPI into the user's wsiTools cache. It keeps
wsiTools CRAN-safe by treating libCZI as an external runtime library and
dynamically loading it through \code{WSITOOLS_LIBCZIAPI}.
}
\details{
ZEISS libCZI is not bundled with wsiTools. It is distributed by ZEISS under a
dual license: LGPL v3 or a commercial/proprietary license from ZEISS. This
helper prints a license notice and requires \code{accept_license = TRUE} before it
runs build commands non-interactively.
}
\examples{
plan <- wsi_install_native_czi(install = FALSE)
plan
}
