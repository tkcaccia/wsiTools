% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/open-viewer.R
\name{wsi_open_fast_viewer}
\alias{wsi_open_fast_viewer}
\title{Open a large image with optimized progressive tiled viewing}
\usage{
wsi_open_fast_viewer(
  input,
  ...,
  live = "yes",
  tiled = "yes",
  dynamic_tiles = "yes",
  tile_prefetch_margin = 1L,
  tile_cache_count = 1024L,
  tile_prefetch_cache_count = 768L,
  progressive_preview = TRUE,
  dynamic_tile_format = "jpg",
  open = interactive(),
  wait = interactive()
)
}
\arguments{
\item{input}{Image path or \code{wsi_slide} object.}

\item{...}{Additional arguments passed to \code{\link[=wsi_open_viewer]{wsi_open_viewer()}}.}

\item{live, tiled, dynamic_tiles}{Viewer routing flags passed to
\code{\link[=wsi_open_viewer]{wsi_open_viewer()}}.}

\item{tile_prefetch_margin}{Number of tile rows/columns beyond the visible
viewport to prefetch.}

\item{tile_cache_count}{Maximum number of OpenSeadragon decoded tiles kept
in the browser cache.}

\item{tile_prefetch_cache_count}{Maximum number of speculative prefetch tile
images kept by wsiTools.}

\item{progressive_preview}{Whether to keep the low-resolution preview behind
the tiled canvas while high-resolution tiles load.}

\item{dynamic_tile_format}{Tile image format for the live dynamic tile
server.}

\item{open, wait}{Browser launch and live bridge servicing options.}
}
\value{
A \code{wsi_viewer_session} for live viewers.
}
\description{
\code{wsi_open_fast_viewer()} is a convenience wrapper for large whole-slide
images. It opens a live, full-resolution tiled viewer, serves tiles on demand
through the local R session, keeps a low-resolution preview visible while
sharper tiles stream in, and increases browser-side tile caching. The full
slide is not loaded into R memory.
}
\examples{
\dontrun{
viewer <- wsi_open_fast_viewer("very_large_slide.svs")
}
}
