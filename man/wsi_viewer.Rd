% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viewer.R
\name{wsi_viewer}
\alias{wsi_viewer}
\title{View a slide interactively}
\usage{
wsi_viewer(
  slide,
  width = 1600,
  height = NULL,
  output = NULL,
  open = interactive(),
  title = NULL,
  overwrite = FALSE,
  mode = c("thumbnail", "tiles"),
  tile_dir = NULL,
  tile_size = 512,
  tile_format = c("jpg", "png"),
  quality = 90,
  rebuild = FALSE,
  tile_overlap = NULL,
  tile_url_base = NULL,
  tile_url_template = NULL,
  tile_url_style = c("deepzoom", "slash"),
  max_level = NULL,
  tile_source_label = NULL,
  tile_sources = NULL,
  roi = NULL,
  roi_fill_alpha = 0.15,
  roi_class_presets = wsi_roi_class_presets(),
  stain = c("none", "ihc", "he"),
  channels = NULL,
  base_layer_name = NULL,
  hematoxylin = c(0.65, 0.704, 0.286),
  hrp = c(0.268, 0.57, 0.776),
  hematoxylin_colour = "#4b3f99",
  hrp_colour = "#8b5a2b",
  hematoxylin_strength = 1,
  hrp_strength = 1,
  segmentation_run_url = NULL,
  viewer_state_url = NULL,
  viewer_state_ws_url = NULL,
  autosave_enabled = FALSE,
  autosave_interval = 5,
  autosave_path = NULL,
  project_images = NULL,
  channel_sources = NULL,
  layers = NULL,
  cellphenotyper = NULL,
  viewer_transport = c("auto", "websocket", "polling")
)
}
\arguments{
\item{slide}{A \code{wsi_slide} object.}

\item{width}{Thumbnail width used for \code{mode = "thumbnail"}.}

\item{height}{Optional thumbnail height limit for \code{mode = "thumbnail"}.}

\item{output}{Optional HTML output path. Defaults to a temporary file.}

\item{open}{Whether to open the viewer with \code{\link[utils:browseURL]{utils::browseURL()}}.}

\item{title}{Optional viewer title.}

\item{overwrite}{Whether to overwrite \code{output} when it already exists.}

\item{mode}{Viewer mode. Use \code{"thumbnail"} for a small self-contained
preview or \code{"tiles"} for a full-resolution Deep Zoom viewer.}

\item{tile_dir}{Directory for Deep Zoom files when \code{mode = "tiles"}.}

\item{tile_size}{Deep Zoom tile size in pixels.}

\item{tile_format}{Tile image format for \code{mode = "tiles"}.}

\item{quality}{JPEG quality for tiled viewers when \code{tile_format = "jpg"}.}

\item{rebuild}{Whether to rebuild existing Deep Zoom tiles.}

\item{tile_overlap}{Optional Deep Zoom tile overlap in pixels. Defaults to
one-pixel overlap for generated static tiles and zero for external tile
sources unless supplied.}

\item{tile_url_base, tile_url_template}{Optional externally served tile source
for \code{mode = "tiles"}. These are normally set by \code{\link[=wsi_viewer_live]{wsi_viewer_live()}} when
\code{dynamic_tiles = TRUE}; when supplied, static Deep Zoom generation is
skipped.}

\item{tile_url_style}{URL style for \code{tile_url_base}: \code{"deepzoom"} uses
\verb{/\{level\}/\{x\}_\{y\}.\{format\}}, while \code{"slash"} uses
\verb{/\{level\}/\{x\}/\{y\}.\{format\}}.}

\item{max_level}{Optional maximum Deep Zoom level for externally served
tiles.}

\item{tile_source_label}{Optional text shown in the viewer subtitle for
externally served tiles.}

\item{tile_sources}{Optional metadata list for project/session tile sources.}

\item{roi}{Optional ROI overlay. Supply a GeoJSON path or an object returned
by \code{\link[=wsi_read_geojson]{wsi_read_geojson()}}. Coordinates are interpreted as level-0 slide
pixel coordinates, matching QuPath-style GeoJSON exports.}

\item{roi_fill_alpha}{Fill transparency for ROI polygons.}

\item{roi_class_presets}{Editable ROI class presets from
\code{\link[=wsi_roi_class_presets]{wsi_roi_class_presets()}}. These define dropdown classes, default class
colours, and optional export rules.}

\item{stain}{Optional stain display mode. Use \code{"ihc"} for interactive
stain-channel color deconvolution in the browser, or \code{"he"} for
hematoxylin/eosin/residual H&E deconvolution.}

\item{channels}{Optional stain channels created by \code{\link[=wsi_stain_channels]{wsi_stain_channels()}}.
RGB brightfield deconvolution supports up to three independent channels at
a time.}

\item{base_layer_name}{Display name for the base image layer in the Stains
menu.}

\item{hematoxylin, hrp}{RGB optical-density vectors used when
\code{stain = "ihc"} and \code{channels} is not supplied.}

\item{hematoxylin_colour, hrp_colour}{Initial display colours for the
hematoxylin and HRP/DAB channels.}

\item{hematoxylin_strength, hrp_strength}{Initial display gains for the
hematoxylin and HRP/DAB channels.}

\item{segmentation_run_url}{Optional HTTP endpoint used by the viewer's
\verb{Run segmentation} button. The browser posts the selected ROI GeoJSON to
this URL and expects either GeoJSON or JSON with a \code{geojson} field in
response. Use
\code{\link[=wsi_stardist_server]{wsi_stardist_server()}} to create a local endpoint.}

\item{viewer_state_url}{Optional HTTP endpoint used to sync annotations,
measurements, segmentation overlays, and display state back to R. Use
\code{\link[=wsi_viewer_live]{wsi_viewer_live()}} or \code{\link[=wsi_viewer_session]{wsi_viewer_session()}} to create this endpoint.}

\item{viewer_state_ws_url}{Optional WebSocket endpoint used by live viewers
for lower-latency browser-to-R sync. The HTTP polling bridge remains the
fallback when this is \code{NULL} or WebSockets are unavailable.}

\item{autosave_enabled}{Whether the viewer should periodically sync state to
the live R bridge for project autosave. This is normally set by
\code{\link[=wsi_viewer_live]{wsi_viewer_live()}}.}

\item{autosave_interval}{Seconds between autosave syncs.}

\item{autosave_path}{Optional project directory shown in autosave status
messages.}

\item{project_images}{Optional paths, slide objects, or project records to
list in the left Project section. CZI paths are listed without loading the
full file into R; real CZI previews require the optional native libCZI
bridge, or an explicitly enabled legacy Python bridge.}

\item{channel_sources}{Optional tiled channel sources created with
\code{\link[=wsi_channel_source]{wsi_channel_source()}}.}

\item{layers}{Optional viewer overlay layers, for example point layers from
CellPhenotyper/StarDist outputs.}

\item{cellphenotyper}{Optional CellPhenotyper project metadata used to enable
the top \strong{Cells} menu.}

\item{viewer_transport}{Browser-to-R live transport advertised to the
viewer: \code{"auto"}, \code{"websocket"}, or \code{"polling"}. Static viewers ignore
this value.}
}
\value{
The HTML viewer path, invisibly.
}
\description{
Creates an HTML viewer for a slide without loading the full whole-slide image
into R memory. The default \code{mode = "thumbnail"} writes a self-contained
thumbnail preview. \code{mode = "tiles"} uses libvips to build Deep Zoom tiles on
disk so zooming can reveal full-resolution detail in the browser.
}
\details{
The viewer includes lightweight pathology-viewer controls inspired by tools
such as QuPath and napari. Controls are grouped into menus for navigation,
annotations, GeoJSON overlays, view aids, and optional stains. These cover
pan and annotation modes, fit and 1:1 zoom, ROI visibility and label toggles, ROI
opacity, ROI previous/next navigation, a left-side annotation manager with
GeoJSON geometry listing, visibility toggles, lock/unlock, colour editing,
name/class editing, duplicate/delete, zoom-to-ROI, selected-ROI export,
browser-side GeoJSON import, crosshair display, polygon drawing,
brush-style annotation painting with a brush-size slider,
selected-ROI extension, platform-aware brush removal (\code{Alt} on Windows/Linux
and \code{Command} on macOS), hole filling, selected-ROI vertex editing,
name/class reassignment including custom annotation categories, 10-step
\code{Ctrl+Z} undo and \code{Ctrl+Shift+Z}/\code{Ctrl+Y} redo for annotations and
trajectories, selected-ROI StarDist export,
optional selected-ROI segmentation runs, segmentation GeoJSON/CSV import,
distance measurement in pixels and micrometres when MPP metadata is
available, browser-based GeoJSON export, and a \code{Ctrl+K} command palette for
common actions such as importing GeoJSON, exporting selected ROIs, running
StarDist, showing a coordinate-only tile grid, and requesting a project save
from a live R session. A \verb{?} shortcut and Help menu show a compact viewer
guide plus the keyboard map for pan, draw, brush, edit, measure, undo, redo,
save, import, and export. The View menu can split tiled slides into 2, 4, or
6 OpenSeadragon panes with linked or independent zoom/pan for comparing
separate tissue regions.
Optional \code{stain = "ihc"} adds an RGB optical-density deconvolution display
with simple original/all/show-only controls.
\code{stain = "he"} uses hematoxylin, eosin, and residual H&E channels. Use
\code{channels} to provide
multi-IHC or custom H&E channel definitions. \code{project_images} adds a
left-side Project section for related images or multi-scene microscopy files,
including CZI entries that can be activated through optional Bio-Formats
tooling.
}
\examples{
\dontrun{
slide <- wsi_open("sample.svs")
html <- wsi_viewer(slide, open = FALSE)
wsi_close(slide)
}
}
