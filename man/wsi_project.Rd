% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{wsi_project}
\alias{wsi_project}
\alias{wsi_save_project}
\alias{save_wsi_project}
\title{Save a reproducible wsiTools project}
\usage{
wsi_project(
  path = NULL,
  slide = NULL,
  viewer_state = NULL,
  rois = NULL,
  measurements = NULL,
  segmentation = NULL,
  stain_settings = NULL,
  stains = NULL,
  tile_manifest = NULL,
  metadata = list(),
  processing_provenance = list(),
  overwrite = FALSE
)

wsi_save_project(
  project,
  path = NULL,
  overwrite = FALSE,
  processing_provenance = NULL
)

save_wsi_project(
  project,
  path = NULL,
  overwrite = FALSE,
  processing_provenance = NULL
)
}
\arguments{
\item{path}{Project directory to create/update, a \code{wsi_slide} object, a live
viewer session/state for an in-memory project, or \code{NULL}.}

\item{slide}{Optional slide path or \code{wsi_slide} object.}

\item{viewer_state}{Optional object from \code{\link[=wsi_viewer_state]{wsi_viewer_state()}} or a live
viewer session/state. ROIs, measurements, segmentation overlays, view
settings, and stain settings are inferred from this object when not
supplied explicitly.}

\item{rois}{Optional \code{wsi_roi} annotations.}

\item{measurements}{Optional measurement data frame, measurement report, or
named list of data frames.}

\item{segmentation}{Optional segmentation result as \code{wsi_roi}, centroid data
frame, or imported segmentation object.}

\item{stain_settings}{Optional list of stain/viewer settings.}

\item{stains}{Optional \code{wsi_ihc_channels} or \code{wsi_stain_channels} object used
to derive stain settings.}

\item{tile_manifest}{Optional tile manifest data frame or named list of
manifests.}

\item{metadata}{Optional user metadata list.}

\item{processing_provenance}{Optional list describing processing steps,
model versions, parameters, commands, or other reproducibility notes.}

\item{overwrite}{Whether to overwrite an existing project index and sidecar
files created by wsiTools.}

\item{project}{A \code{wsi_project} object, or a compatible list.}
}
\value{
A \code{wsi_project} object. If saved, the object contains the project
index and loaded in-memory objects.
}
\description{
Creates an editable project object, or writes one directly when \code{path} is a
project directory. The project stores slide identity, viewer state, ROI
annotations, measurements, segmentation outputs, stain settings, tile
manifests, and processing provenance as a JSON index plus sidecar
GeoJSON/CSV files. Pixel data are not copied or loaded.
}
\examples{
\dontrun{
slide <- wsi_open("sample.svs")
project <- wsi_project(slide)
project$rois <- viewer$get_rois()
project$measurements <- viewer$get_measurements()
project$segmentation <- viewer$get_segmentation()
wsi_save_project(project, "case_001.wsiproject")

rois <- read_geojson("annotations.geojson")
tiles <- extract_tiles(slide, roi = rois, save_images = FALSE)
project <- wsi_project(
  "case_001.wsiproject",
  slide = slide,
  rois = rois,
  tile_manifest = tiles
)
wsi_close(slide)
reopened <- wsi_read_project("case_001.wsiproject")
}
}
