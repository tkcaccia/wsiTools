% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-clusters.R
\name{wsi_spatial_cluster_fields}
\alias{wsi_spatial_cluster_fields}
\alias{wsi_spatial_clusters}
\title{Inspect spatial-object clustering metadata}
\usage{
wsi_spatial_cluster_fields(object, spot_ids = NULL)

wsi_spatial_clusters(object, spot_ids = NULL, field = NULL)
}
\arguments{
\item{object}{A Seurat, Giotto, SpatialExperiment, linked wsiTools spatial
object, or compatible list-like object.}

\item{spot_ids}{Optional spot/cell identifiers used to align metadata to the
viewer spot order.}

\item{field}{Optional cluster field name. When \code{NULL}, all detected fields
are returned.}
}
\value{
\code{wsi_spatial_cluster_fields()} returns a data frame describing
available fields. \code{wsi_spatial_clusters()} returns one row per spot/cell
with aligned cluster values.
}
\description{
These helpers look for already-computed clustering, class, or annotation
fields stored inside Seurat, Giotto-like, and SpatialExperiment-like objects.
They inspect metadata tables only; expression matrices and whole-slide images
are not copied into the browser or loaded into memory.
}
\examples{
obj <- list(meta.data = data.frame(
  seurat_clusters = c("0", "1"),
  row.names = c("spot_a", "spot_b")
))
wsi_spatial_cluster_fields(obj, spot_ids = c("spot_a", "spot_b"))
}
