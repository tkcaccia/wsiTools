% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell-counts.R
\name{wsi_cell_counts}
\alias{wsi_cell_counts}
\alias{cell_counts}
\title{Build per-cell count and channel measurement tables}
\usage{
wsi_cell_counts(
  slide = NULL,
  segmentation,
  channels = NULL,
  rois = NULL,
  image_origin = c(x = 0, y = 0),
  channel_downsample = 1,
  channel = NULL,
  cell_radius = 0,
  positive_threshold = NULL,
  pixel_size = NULL,
  file = NULL,
  output_dir = NULL,
  prefix = "wsi_cell_counts",
  overwrite = FALSE
)

cell_counts(
  slide = NULL,
  segmentation,
  channels = NULL,
  rois = NULL,
  image_origin = c(x = 0, y = 0),
  channel_downsample = 1,
  channel = NULL,
  cell_radius = 0,
  positive_threshold = NULL,
  pixel_size = NULL,
  file = NULL,
  output_dir = NULL,
  prefix = "wsi_cell_counts",
  overwrite = FALSE
)
}
\arguments{
\item{slide}{Optional \code{wsi_slide} object. Used only to infer microns per
pixel via \code{\link[=wsi_mpp]{wsi_mpp()}} when \code{pixel_size} is not supplied.}

\item{segmentation}{Cell segmentation as a centroid table, \code{wsi_roi}
polygons, imported segmentation object, or file path.}

\item{channels}{Optional \code{wsi_ihc_channels} object, or a named list of
numeric channel matrices, representing a patch/region/thumbnail already in
memory.}

\item{rois}{Optional \code{wsi_roi} annotations, or a GeoJSON file path. Cells are
assigned to the first ROI containing their centroid.}

\item{image_origin}{Level-0 x/y coordinate of the top-left pixel in
\code{channels}.}

\item{channel_downsample}{Slide pixels represented by one channel-matrix
pixel. Use values greater than 1 when \code{channels} came from a thumbnail or
lower-resolution pyramid level.}

\item{channel}{Optional channel ids or names to measure.}

\item{cell_radius}{Radius, in slide pixels, used to average channel values
around each centroid. The default \code{0} samples the centroid pixel only.}

\item{positive_threshold}{Optional numeric threshold(s) used to add per-cell
channel-positive flags.}

\item{pixel_size}{Optional microns per pixel. When omitted and \code{slide} is a
slide object, \code{\link[=wsi_mpp]{wsi_mpp()}} is used when available.}

\item{file}{Optional CSV path for the per-cell table.}

\item{output_dir}{Optional directory where per-cell, ROI, class, and counts
matrix CSV files are written.}

\item{prefix}{File prefix used with \code{output_dir}.}

\item{overwrite}{Whether to overwrite existing CSV files.}
}
\value{
A \code{wsi_cell_counts} list with \code{cell_table}, \code{counts_matrix},
\code{roi_counts}, \code{class_counts}, and \code{files}.
}
\description{
Combines imported segmentation with optional deconvolved stain channels and
optional ROI annotations. The function works from centroid coordinates,
polygon cell segmentations, or imported external outputs. It
samples only the already-supplied channel matrices and never loads a whole
slide into memory.
}
\examples{
cells <- data.frame(cell_id = c("c1", "c2"), x = c(5.5, 20.5), y = c(5.5, 6.5))
channels <- structure(
  list(
    hematoxylin = matrix(0.2, nrow = 32, ncol = 32),
    hrp_dab = matrix(0.6, nrow = 32, ncol = 32),
    channel_metadata = list(
      list(id = "hematoxylin", name = "Hematoxylin"),
      list(id = "hrp_dab", name = "HRP/DAB")
    )
  ),
  class = "wsi_ihc_channels"
)
counts <- wsi_cell_counts(segmentation = cells, channels = channels)
}
