% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stain.R
\name{wsi_deconvolve_ihc}
\alias{wsi_deconvolve_ihc}
\title{Deconvolve hematoxylin and HRP/DAB stains from an IHC image}
\usage{
wsi_deconvolve_ihc(
  image,
  format = c("channels", "array", "raster", "magick"),
  hematoxylin = c(0.65, 0.704, 0.286),
  hrp = c(0.268, 0.57, 0.776),
  hematoxylin_colour = "#4b3f99",
  hrp_colour = "#8b5a2b",
  hematoxylin_strength = 1,
  hrp_strength = 1,
  epsilon = 1/255
)
}
\arguments{
\item{image}{RGB/RGBA array, raster object, or magick image.}

\item{format}{Output format. \code{"channels"} returns numeric concentration
matrices for \code{hematoxylin} and \code{hrp}; image formats return a recolored
two-channel visualization.}

\item{hematoxylin, hrp}{RGB optical-density vectors for hematoxylin and
HRP/DAB. Defaults are conventional H-DAB stain vectors.}

\item{hematoxylin_colour, hrp_colour}{Display colours for recolored outputs.}

\item{hematoxylin_strength, hrp_strength}{Display gains for recolored outputs.}

\item{epsilon}{Lower bound used before taking optical-density logarithms.}
}
\value{
A \code{wsi_ihc_channels} object, array, raster, or magick image.
}
\description{
Performs color deconvolution on an already-small image object such as a
thumbnail or a region read with \code{\link[=wsi_read_region]{wsi_read_region()}}. The implementation uses
the standard H-DAB optical-density vectors by default and returns separate
hematoxylin and HRP/DAB concentration channels, or a recolored pseudo-image.
}
\details{
This function is intended for patches, regions, and thumbnails. It does not
read a whole-slide image into memory.
}
\examples{
patch <- array(0.8, dim = c(32, 32, 3))
channels <- wsi_deconvolve_ihc(patch)
}
