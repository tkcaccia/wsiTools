% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-objects.R
\name{wsi_viewer_spatialexperiment_project}
\alias{wsi_viewer_spatialexperiment_project}
\title{Open a multi-slide SpatialExperiment project in the wsiTools viewer}
\usage{
wsi_viewer_spatialexperiment_project(
  spe = NULL,
  images = NULL,
  linked = NULL,
  sample_ids = NULL,
  image_names = NULL,
  labels = NULL,
  assay_name = NULL,
  reduction = "PCA",
  dims = c(1L, 2L),
  coordinate_scale = c("auto", "none", "fullres", "hires", "lowres", "seurat_image",
    "custom"),
  scale_x = NULL,
  scale_y = NULL,
  coordinate_flip = c("none", "vertical", "horizontal"),
  coordinate_rotation = c(0, 90, 180, 270),
  coordinate_transform = "none",
  spot_genes = NULL,
  default_gene = NULL,
  spot_radius = NULL,
  max_points = 100000L,
  colour_by = c("component_1", "gene", "none"),
  mode = c("tiles", "thumbnail"),
  live = TRUE,
  dynamic_tiles = FALSE,
  wait = interactive(),
  transport = c("auto", "websocket", "polling"),
  output = NULL,
  open = interactive(),
  overwrite = FALSE,
  title = "wsiTools SpatialExperiment project viewer",
  width = 1600,
  height = NULL,
  tile_dir = NULL,
  tile_size = 512,
  tile_format = c("jpg", "png"),
  quality = 90,
  rebuild = FALSE,
  tile_overlap = NULL,
  roi_class_presets = wsi_roi_class_presets()
)
}
\arguments{
\item{spe}{A SpatialExperiment object, or SpatialExperiment-like object with
\code{colData()}, \code{spatialCoords()}, and \code{reducedDims()}.}

\item{images}{Named character vector/list of high-resolution image paths or
\code{wsi_slide} objects. Names are used as \code{sample_id} values when
\code{sample_ids} is not supplied.}

\item{linked}{Optional pre-linked spatial sections. Supplying \code{linked}
skips the linking step.}

\item{sample_ids}{Sample identifiers matching a \code{sample_id}, \code{sample},
\code{section}, or \code{image_id} column in \code{colData(spe)}. When \code{NULL},
names on \code{images} are used; if those are missing, unique sample IDs are
inferred from \code{colData(spe)} when their number matches \code{images}.}

\item{image_names}{Optional image/section names stored in the linked objects.
Defaults to \code{sample_ids}.}

\item{labels}{Optional labels shown in the Project panel.}

\item{assay_name}{Optional assay name used for live gene expression lookup.}

\item{reduction}{Dimensional reduction to extract, for example \code{"PCA"}.}

\item{dims}{Two reduction dimensions to plot.}

\item{coordinate_scale}{How to map spatial coordinates onto \code{image}.
\code{"auto"} rescales from stored preview dimensions when available.
\code{"none"} uses coordinates as supplied. \code{"custom"} uses
\code{scale_x} and \code{scale_y}.}

\item{scale_x, scale_y}{Custom coordinate scale factors used when
\code{coordinate_scale = "custom"}.}

\item{coordinate_flip}{Orientation flip applied after coordinate scaling and
before rotation. Choices are \code{"none"}, \code{"vertical"}, and
\code{"horizontal"}.}

\item{coordinate_rotation}{Clockwise rotation applied after
\code{coordinate_flip}. Choices are \code{0}, \code{90}, \code{180}, and
\code{270} degrees.}

\item{coordinate_transform}{Legacy one-step orientation transform kept for
backwards compatibility. Prefer \code{coordinate_flip} and
\code{coordinate_rotation} for new code.}

\item{spot_genes}{Optional character vector of gene names to extract and send
to the viewer for spot colouring. Only these genes are embedded in static HTML
viewers.}

\item{default_gene}{Optional gene name used as the initial spot colour
variable.}

\item{spot_radius}{Spot marker radius, in slide pixels.}

\item{max_points}{Maximum number of spots to keep in the browser payload.}

\item{colour_by}{Spot colour mode. \code{"component_1"} colours by the first
plotted reduction component; \code{"gene"} colours by \code{default_gene};
\code{"none"} uses one colour.}

\item{mode}{Viewer mode. \code{"tiles"} uses prebuilt Deep Zoom tiles and is
recommended for high-resolution tissue images. \code{"thumbnail"} is useful for
quick tests and mock slides.}

\item{live}{Use \code{\link[=wsi_viewer_live]{wsi_viewer_live()}} by default so the project can synchronize
annotations, selections, measurements, and spot tables back to R. Set
\code{live = FALSE} to write a static HTML viewer.}

\item{dynamic_tiles}{When \code{live = TRUE}, whether the first active image should
be served from the dynamic tile server instead of the prebuilt tile source.
The default is \code{FALSE} because prebuilt project tiles are usually smoother.}

\item{wait}{For live viewers, whether to keep servicing the local R/httpuv
bridge until interrupted.}

\item{transport}{Live viewer transport passed to \code{\link[=wsi_viewer_live]{wsi_viewer_live()}}.}

\item{output, open, overwrite, title}{Viewer output options.}

\item{width, height}{Thumbnail/navigator size options passed to the viewer.}

\item{tile_dir}{Directory where per-image Deep Zoom tile pyramids are stored.}

\item{tile_size, tile_format, quality, rebuild, tile_overlap}{Deep Zoom tiling
options used in tiled mode.}

\item{roi_class_presets}{ROI classes used by the annotation UI.}
}
\value{
A \code{wsi_viewer_session} by default. If \code{live = FALSE}, returns the
static HTML path.
}
\description{
\code{wsi_viewer_spatialexperiment_project()} links a multi-sample
\code{SpatialExperiment} object to one high-resolution tissue image per sample
and opens the sections as a single wsiTools project. Each project item keeps
its own spot overlay and only the dimensional-reduction buttons that are
present in the object, such as PCA, t-SNE, UMAP, or custom reductions.
}
\examples{
\dontrun{
images <- c(
  `151507` = "151507_full_image.tif",
  `151508` = "151508_full_image.tif"
)
viewer <- wsi_viewer_spatialexperiment_project(
  spe,
  images = images,
  coordinate_scale = "none",
  output = "spatialexperiment_project.html",
  wait = FALSE
)
}
}
