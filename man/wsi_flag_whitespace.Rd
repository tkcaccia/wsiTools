% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/whitespace-filter.R
\name{wsi_flag_whitespace}
\alias{wsi_flag_whitespace}
\alias{wsi_filter_whitespace_tiles}
\title{Flag or filter whitespace/background tiles in a grid}
\usage{
wsi_flag_whitespace(
  slide,
  grid,
  options = wsi_whitespace_options(),
  action = c("flag", "drop")
)

wsi_filter_whitespace_tiles(
  slide,
  grid,
  options = wsi_whitespace_options(),
  action = c("drop", "flag")
)
}
\arguments{
\item{slide}{A \code{wsi_slide} object.}

\item{grid}{Tile grid from \code{\link[=wsi_tile_grid]{wsi_tile_grid()}} or \code{\link[=extract_tiles]{extract_tiles()}}.}

\item{options}{Thresholds from \code{\link[=wsi_whitespace_options]{wsi_whitespace_options()}}.}

\item{action}{\code{"flag"} keeps all rows and appends columns; \code{"drop"} removes
rows where \code{whitespace_flag} is \code{TRUE}.}
}
\value{
A tile grid or manifest with whitespace/background metrics.
}
\description{
Reads only the regions listed in \code{grid}, computes whitespace/background
metrics for each tile, appends manifest columns, and optionally drops tiles
where \code{whitespace_flag} is \code{TRUE}. This is region-based and never reads a
whole-slide image into memory.
}
