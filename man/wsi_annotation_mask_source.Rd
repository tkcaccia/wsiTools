% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask-annotations.R
\name{wsi_annotation_mask_source}
\alias{wsi_annotation_mask_source}
\title{Create an editable tiled tissue-annotation mask source}
\usage{
wsi_annotation_mask_source(
  mask,
  slide,
  legend = NULL,
  name = "Tissue annotation mask",
  id = NULL,
  opacity = 0.55,
  visible = TRUE,
  tile_size = 512,
  cache_dir = NULL,
  persistent_cache = TRUE,
  target_path = NULL
)
}
\arguments{
\item{mask}{TIFF or OME-TIFF mask path readable by libvips.}

\item{slide}{Base microscopy slide or image path used to define the mask's
placement in level-0 slide coordinates.}

\item{legend}{Optional sidecar label information. Supply a CSV path, data
frame, or list of entries with value/label/colour fields. When \code{NULL},
wsiTools looks for \verb{<mask>_labels.csv}, \verb{<mask>.labels.csv}, or \code{labels.csv}.}

\item{name, id}{Display name and stable source identifier.}

\item{opacity, visible}{Initial display settings.}

\item{tile_size}{Dynamic mask tile size.}

\item{cache_dir}{Optional tile-cache directory.}

\item{persistent_cache}{Keep generated mask tiles in the fingerprinted cache.}

\item{target_path}{Optional project image path. Defaults to the base slide
path and keeps the mask attached to the correct image in multi-view.}
}
\value{
A \code{wsi_dynamic_annotation_mask_tile_source} suitable for
\code{annotation_masks} or \code{channel_sources} in \code{\link[=wsi_viewer_live]{wsi_viewer_live()}}.
}
\description{
Opens a TIFF/OME-TIFF annotation mask as a lazy dynamic tile source for
\code{\link[=wsi_viewer_live]{wsi_viewer_live()}}. The complete mask is not loaded into R or the browser.
Brush and Magic Wand edits are stored as sparse raster delta tiles in the
viewer, avoiding polygon Boolean operations on every pointer movement.
}
