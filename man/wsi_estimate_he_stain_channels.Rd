% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stain.R
\name{wsi_estimate_he_stain_channels}
\alias{wsi_estimate_he_stain_channels}
\title{Estimate H&E stain channel definitions from a slide or thumbnail}
\usage{
wsi_estimate_he_stain_channels(
  x,
  method = c("macenko", "vahadane", "fixed"),
  thumbnail_width = 2048,
  include_residual = TRUE,
  hematoxylin_colour = "#4b3f99",
  eosin_colour = "#e85b90",
  residual_colour = "#6b7280",
  hematoxylin_strength = 1,
  eosin_strength = 1,
  residual_strength = 1,
  residual_visible = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{wsi_slide}, slide path, RGB/RGBA array, raster object, or magick
image. Slide inputs are sampled through \code{\link[=wsi_thumbnail]{wsi_thumbnail()}}.}

\item{method}{Stain-vector estimation method. \code{"macenko"} is usually a good
first choice for H&E. \code{"vahadane"} is an experimental dependency-free NMF
estimate. \code{"fixed"} uses the standard H&E vectors without reading a
thumbnail.}

\item{thumbnail_width}{Width of the low-resolution thumbnail used when \code{x} is
a slide or path.}

\item{include_residual}{Include a residual optical-density channel.}

\item{hematoxylin_colour, eosin_colour}{Display colours for recoloured
channel visualisation.}

\item{residual_colour}{Display colour for the residual channel.}

\item{hematoxylin_strength, eosin_strength, residual_strength}{Initial display
gains for the channels.}

\item{residual_visible}{Whether the residual channel is visible by default in
the interactive viewer and recoloured image outputs.}

\item{...}{Additional arguments passed to \code{\link[=wsi_estimate_stain_matrix]{wsi_estimate_stain_matrix()}}, such
as \code{luminosity_threshold}, \code{od_threshold}, or \code{max_pixels}.}
}
\value{
A \code{wsi_stain_channels} object with slide-specific hematoxylin, eosin,
and optionally residual channel definitions.
}
\description{
Estimates slide-specific hematoxylin/eosin optical-density vectors from a
low-resolution thumbnail or already-small H&E image, then returns channel
definitions suitable for \code{\link[=wsi_deconvolve_he]{wsi_deconvolve_he()}}, \code{\link[=wsi_viewer_he]{wsi_viewer_he()}}, and live
tiled stain-channel viewing. When \code{x} is a \code{wsi_slide}, only a thumbnail is
read; the level-0 whole-slide image is never loaded into R memory.
}
\examples{
\dontrun{
slide <- wsi_open("he_sample.svs")
channels <- wsi_estimate_he_stain_channels(slide, method = "macenko")
html <- wsi_viewer_he(slide, channels = channels, open = FALSE)
wsi_close(slide)
}
}
