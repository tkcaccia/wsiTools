% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measurements.R
\name{measure_ihc_intensity}
\alias{measure_ihc_intensity}
\alias{wsi_measure_ihc_intensity}
\alias{summarise_ihc_intensity}
\alias{wsi_summarise_ihc_intensity}
\title{Measure IHC intensity inside ROIs}
\usage{
measure_ihc_intensity(
  channels,
  rois = NULL,
  image_origin = c(x = 0, y = 0),
  dab_channel = NULL,
  hematoxylin_channel = NULL,
  dab_threshold = 0.1,
  pixel_size = NULL,
  by = c("roi", "class", "both"),
  file = NULL,
  overwrite = FALSE,
  max_pixels = 5e+06
)

wsi_measure_ihc_intensity(
  channels,
  rois = NULL,
  image_origin = c(x = 0, y = 0),
  dab_channel = NULL,
  hematoxylin_channel = NULL,
  dab_threshold = 0.1,
  pixel_size = NULL,
  by = c("roi", "class", "both"),
  file = NULL,
  overwrite = FALSE,
  max_pixels = 5e+06
)

summarise_ihc_intensity(x, file = NULL, overwrite = FALSE)

wsi_summarise_ihc_intensity(x, file = NULL, overwrite = FALSE)
}
\arguments{
\item{channels}{A \code{wsi_ihc_channels} object returned by
\code{\link[=wsi_deconvolve_ihc]{wsi_deconvolve_ihc()}} or \code{\link[=wsi_deconvolve_multi_ihc]{wsi_deconvolve_multi_ihc()}}.}

\item{rois}{Optional \code{wsi_roi} object. When omitted, the whole supplied
channel image is summarised.}

\item{image_origin}{Level-0 x/y coordinate of the top-left pixel in
\code{channels}.}

\item{dab_channel, hematoxylin_channel}{Optional channel ids or names. Defaults
match the standard \code{hrp_dab} and \code{hematoxylin} channels.}

\item{dab_threshold}{DAB concentration threshold for positive-area reporting.
This is assay-specific; tune it for your staining and scanner.}

\item{pixel_size}{Optional microns per pixel. Used to report positive area in
square microns and square millimetres.}

\item{by}{Return level: \code{"roi"}, \code{"class"}, or \code{"both"}.}

\item{file}{Optional CSV output path. When \code{by = "both"}, this is treated as
an output directory and two CSV files are written.}

\item{overwrite}{Whether to overwrite output files.}

\item{max_pixels}{Maximum ROI candidate pixels to mask in one call.}

\item{x}{ROI-level IHC intensity data returned by
\code{\link[=measure_ihc_intensity]{measure_ihc_intensity()}}.}
}
\value{
A data frame, or a \code{wsi_ihc_intensity_report} list when
\code{by = "both"}.
}
\description{
Computes practical hematoxylin/HRP-DAB measurements inside ROI polygons from
deconvolved IHC channels. The function works on already-read regions,
thumbnails, or crops represented by a \code{wsi_ihc_channels} object; it does not
read a whole-slide image into memory.
}
\details{
Reported ROI metrics include DAB mean, DAB-positive area, hematoxylin
density, and the DAB/hematoxylin ratio. Class summaries use pixel-count
weighted means and summed DAB-positive area.
}
\examples{
image <- array(0.8, dim = c(50, 50, 3))
channels <- wsi_deconvolve_ihc(image)
measure_ihc_intensity(channels, dab_threshold = 0.1)
}
