% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viewer-project.R
\name{wsi_viewer_czi_project_live}
\alias{wsi_viewer_czi_project_live}
\title{Open CZI files as a live full-resolution sectioned project viewer}
\usage{
wsi_viewer_czi_project_live(
  images,
  output = NULL,
  open = interactive(),
  width = 1024,
  title = "wsiTools CZI full-resolution project viewer",
  overwrite = FALSE,
  tile_size = 512,
  tile_overlap = 1,
  tile_format = c("jpg", "png", "jpeg"),
  channel = 0,
  sections = TRUE,
  czi_preview = c("lazy", "all"),
  host = "127.0.0.1",
  port = 8798,
  path = "/viewer-state",
  max_tries = 20L,
  transport = c("auto", "websocket", "polling"),
  tile_path = "/tiles",
  cache_dir = NULL,
  name = "wsi_czi_project_live_state",
  envir = parent.frame(),
  wait = interactive(),
  roi_class_presets = wsi_roi_class_presets()
)
}
\arguments{
\item{images}{Character vector of CZI file paths.}

\item{output}{HTML file to write. When \code{NULL}, a temporary HTML file is used.}

\item{open}{Open the generated HTML file in a browser.}

\item{width}{Preview width for the navigator/Project panel thumbnails when
\code{czi_preview = "all"}.}

\item{title}{Viewer title.}

\item{overwrite}{Overwrite \code{output} if it already exists.}

\item{tile_size, tile_overlap}{OpenSeadragon tile size and overlap.}

\item{tile_format}{Dynamic tile format.}

\item{channel}{Zero-based CZI channel shown as the RGB base image.}

\item{sections}{Show detected CZI scenes/sections separately in the Project
panel. Set to \code{FALSE} to expose the whole CZI bounding box as one tiled
image.}

\item{czi_preview}{Preview strategy for CZI scenes. \code{"lazy"} (default)
avoids reading preview pixels before the viewer opens and uses the live
tile server for the selected scene. \code{"all"} preserves the older behavior
and generates low-resolution previews for all scenes before opening.}

\item{host, port, path, max_tries}{Local HTTP/WebSocket bridge address.}

\item{transport}{Live browser-to-R transport.}

\item{tile_path}{HTTP route used for dynamic tiles.}

\item{cache_dir}{Optional dynamic tile cache directory.}

\item{name, envir}{Live state name and environment.}

\item{wait}{If \code{TRUE}, service the live bridge until interrupted.}

\item{roi_class_presets}{ROI class presets used by the annotation UI.}
}
\value{
A \code{wsi_viewer_session} object, invisibly.
}
\description{
\code{wsi_viewer_czi_project_live()} is the full-resolution companion to
\code{\link[=wsi_viewer_project]{wsi_viewer_project()}} for Zeiss CZI files. It starts a local \code{httpuv}
bridge and serves each detected CZI scene/section as OpenSeadragon tiles
generated on demand by the optional native CZI backend. Only requested tiles
are read, so the complete CZI image is not loaded into R memory.
}
\details{
The returned \code{wsi_viewer_session} must stay alive for the browser to request
tiles. Use \code{wait = TRUE} for a simple blocking live viewer, or keep the
returned session object in R and call \code{\link[=wsi_viewer_service]{wsi_viewer_service()}} from long-running
scripts when your R front-end does not service \code{httpuv} automatically.
}
\examples{
\dontrun{
session <- wsi_viewer_czi_project_live(c("scene_1.czi", "scene_2.czi"))
}
}
