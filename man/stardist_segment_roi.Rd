% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmentation.R
\name{stardist_segment_roi}
\alias{stardist_segment_roi}
\alias{wsi_stardist_segment_roi}
\title{Run StarDist on a selected ROI}
\usage{
stardist_segment_roi(
  image,
  roi,
  output_dir,
  roi_id = NULL,
  level = 0,
  crop_format = c("png", "tiff", "jpeg"),
  model = "2D_versatile_he",
  command = NULL,
  args = NULL,
  output = NULL,
  output_type = c("auto", "geojson", "csv", "mask"),
  prob_thresh = NULL,
  nms_thresh = NULL,
  translate_geojson = TRUE,
  overwrite = FALSE,
  run = TRUE,
  backend = c("auto", "vips", "openslide")
)

wsi_stardist_segment_roi(
  image,
  roi,
  output_dir,
  roi_id = NULL,
  level = 0,
  crop_format = c("png", "tiff", "jpeg"),
  model = "2D_versatile_he",
  command = NULL,
  args = NULL,
  output = NULL,
  output_type = c("auto", "geojson", "csv", "mask"),
  prob_thresh = NULL,
  nms_thresh = NULL,
  translate_geojson = TRUE,
  overwrite = FALSE,
  run = TRUE,
  backend = c("auto", "vips", "openslide")
)
}
\arguments{
\item{image}{A \code{wsi_slide} object or a path that can be opened by
\code{\link[=wsi_open]{wsi_open()}}.}

\item{roi}{A \code{wsi_roi} object containing the selected ROI.}

\item{output_dir}{Directory for the ROI crop and StarDist output.}

\item{roi_id}{Optional ROI id. When omitted, the first ROI is used.}

\item{level}{Pyramid level used for the exported crop.}

\item{crop_format}{Crop image format.}

\item{model}{StarDist model identifier passed into \code{{model}} placeholders.}

\item{command}{External command. If omitted, uses
\code{WSITOOLS_STARDIST_COMMAND} or \code{stardist-predict2d} when available.}

\item{args}{Command arguments. Placeholders \code{{input}}, \code{{output}}, \code{{model}},
\code{{prob_thresh}}, and \code{{nms_thresh}} are substituted safely before calling
\code{\link[=system2]{system2()}}.}

\item{output}{Optional expected StarDist output path. Defaults to a GeoJSON
path in \code{output_dir}.}

\item{output_type}{Output type for \code{\link[=import_segmentation]{import_segmentation()}}.}

\item{prob_thresh, nms_thresh}{Optional StarDist thresholds.}

\item{translate_geojson}{Whether to translate crop-local GeoJSON polygons to
slide coordinates using the selected ROI bounding box origin.}

\item{overwrite}{Whether to overwrite \code{output}.}

\item{run}{If \code{FALSE}, return the command plan without running StarDist.}

\item{backend}{Region export backend.}
}
\value{
A \code{wsi_stardist_result} object.
}
\description{
Exports the bounding box of a selected ROI, runs an optional external
StarDist command on the crop, imports the segmentation output, and for
GeoJSON outputs maps crop-local coordinates back to level-0 slide
coordinates. Centroid CSV/TSV outputs are also mapped back to slide
coordinates when \code{translate_geojson = TRUE}.
}
