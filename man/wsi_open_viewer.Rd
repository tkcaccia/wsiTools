% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/open-viewer.R
\name{wsi_open_viewer}
\alias{wsi_open_viewer}
\title{Open an image in the viewer with one command}
\usage{
wsi_open_viewer(
  input,
  ...,
  backend = c("auto", "openslide", "vips", "native_czi", "bioformats", "omezarr",
    "imagemagick"),
  live = c("auto", "yes", "no"),
  tiled = c("auto", "yes", "no"),
  dynamic_tiles = c("auto", "yes", "no"),
  renderer = c("auto", "gpu", "cpu"),
  open = interactive(),
  wait = NULL,
  output = NULL,
  overwrite = FALSE,
  close_slide = TRUE,
  czi_sections = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{input}{Image path, character vector of image paths, or an existing
\code{wsi_slide} object.}

\item{...}{Additional arguments passed to \code{\link[=wsi_viewer]{wsi_viewer()}},
\code{\link[=wsi_viewer_live]{wsi_viewer_live()}}, \code{\link[=wsi_viewer_project]{wsi_viewer_project()}}, or
\code{\link[=wsi_viewer_czi_project_live]{wsi_viewer_czi_project_live()}}, depending on the selected route.}

\item{backend}{Backend passed to \code{\link[=wsi_open]{wsi_open()}} for non-CZI single-image paths.}

\item{live}{\code{"auto"}, \code{"yes"}, or \code{"no"}. \code{"auto"} uses live mode only in
interactive sessions when the optional \code{httpuv} package is available.}

\item{tiled}{\code{"auto"}, \code{"yes"}, or \code{"no"}. \code{"auto"} uses tiled mode when
feasible for the selected viewer route.}

\item{dynamic_tiles}{\code{"auto"}, \code{"yes"}, or \code{"no"}. Dynamic tiles require live
mode. \code{"auto"} uses them when prebuilt libvips Deep Zoom tiles are not the
better default.}

\item{renderer}{Base-image renderer passed to every selected viewer route.
\code{"auto"} uses GPU-accelerated OpenSeadragon when available with a Canvas
fallback; \code{"gpu"} requires WebGL and \code{"cpu"} forces Canvas. Tauri and direct
R launches use the same option.}

\item{open}{Whether to open the viewer in a browser.}

\item{wait}{For live viewers, whether to service the local \code{httpuv} bridge
until interrupted. The default uses \code{TRUE} only for interactive live runs.}

\item{output}{Optional HTML output path.}

\item{overwrite}{Whether to overwrite \code{output}.}

\item{close_slide}{For static viewers opened from a path, close the slide
after writing the HTML. Live viewers keep the slide in the returned session.}

\item{czi_sections}{For CZI paths, show detected scenes/sections separately
in the Project panel.}

\item{quiet}{If \code{FALSE}, print the route selected by wsiTools.}
}
\value{
A \code{wsi_viewer_session} for live viewers, otherwise the HTML path.
}
\description{
\code{wsi_open_viewer()} is the friendliest entry point for first-time use:
supply an image path and let wsiTools choose the backend, viewer type,
tiled/full-resolution mode, and browser launch behavior. It does not load a
complete whole-slide image into R memory.
}
\details{
The automatic choices are conservative:
\itemize{
\item live mode is used in interactive R sessions when \code{httpuv} is
installed; otherwise a static HTML viewer is written;
\item tiled mode is used for real file-backed slides when a tiled route is
available; mock slides and unsupported cases use thumbnail mode;
\item prebuilt Deep Zoom tiles are preferred for libvips-readable slides;
live dynamic tiles are used for very large files, when prebuilt libvips
Deep Zoom tiles are not the better default, or when explicitly requested;
\item CZI paths are opened through the existing section-aware project
viewer helpers.
}
}
\examples{
\dontrun{
viewer <- wsi_open_viewer("sample.svs")

# Force a static HTML viewer:
html <- wsi_open_viewer("sample.svs", live = "no", open = FALSE)
}
}
