% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measurements.R
\name{measure_stain_intensity}
\alias{measure_stain_intensity}
\alias{wsi_measure_stain_intensity}
\title{Summarise stain intensity in deconvolved IHC channels}
\usage{
measure_stain_intensity(
  channels,
  rois = NULL,
  image_origin = c(x = 0, y = 0),
  channel = NULL,
  positive_threshold = NULL,
  file = NULL,
  overwrite = FALSE,
  max_pixels = 5e+06
)

wsi_measure_stain_intensity(
  channels,
  rois = NULL,
  image_origin = c(x = 0, y = 0),
  channel = NULL,
  positive_threshold = NULL,
  file = NULL,
  overwrite = FALSE,
  max_pixels = 5e+06
)
}
\arguments{
\item{channels}{A \code{wsi_ihc_channels} object returned by
\code{\link[=wsi_deconvolve_ihc]{wsi_deconvolve_ihc()}} or \code{\link[=wsi_deconvolve_multi_ihc]{wsi_deconvolve_multi_ihc()}}.}

\item{rois}{Optional \code{wsi_roi} object. When supplied, intensities are
summarised inside each ROI.}

\item{image_origin}{Level-0 x/y coordinate of the top-left pixel in
\code{channels}. Use this when \code{channels} came from a region crop but \code{rois}
are in slide coordinates.}

\item{channel}{Optional channel ids or names to summarise.}

\item{positive_threshold}{Optional concentration threshold used to report
positive pixel fraction.}

\item{file}{Optional CSV output path.}

\item{overwrite}{Whether to overwrite \code{file}.}

\item{max_pixels}{Maximum ROI candidate pixels to mask in one call.}
}
\value{
A data frame with per-channel or per-ROI stain intensity summaries.
}
\description{
Summarises hematoxylin, HRP/DAB, or other deconvolved stain-channel
concentration matrices for a small image, thumbnail, or region. The function
works on a \code{wsi_ihc_channels} object and does not read whole-slide pixels.
}
