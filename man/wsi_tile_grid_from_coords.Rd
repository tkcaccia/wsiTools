% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tile-grid.R
\name{wsi_tile_grid_from_coords}
\alias{wsi_tile_grid_from_coords}
\title{Generate a tile grid from explicit coordinates}
\usage{
wsi_tile_grid_from_coords(
  slide,
  coords,
  tile_size = 512,
  level = 0,
  anchor = c("top_left", "center"),
  bounds = c("error", "trim", "drop"),
  tissue_mask = NULL
)
}
\arguments{
\item{slide}{A \code{wsi_slide} object.}

\item{coords}{Data frame, matrix, or list with at least \code{x} and \code{y}. Optional
columns include \code{width}, \code{height}, \code{level}, \code{tile_id}, \code{output_file}, and
\code{roi_id}.}

\item{tile_size}{Default tile width and height in pixels at \code{level} when
\code{coords$width} or \code{coords$height} are absent.}

\item{level}{Default pyramid level when \code{coords$level} is absent.}

\item{anchor}{Whether coordinates mark the tile \code{"top_left"} or \code{"center"}.}

\item{bounds}{How to handle tiles outside slide bounds: \code{"error"} aborts,
\code{"trim"} clips partial edge tiles, and \code{"drop"} removes out-of-bounds
tiles.}

\item{tissue_mask}{Optional \code{wsi_tissue_mask} object.}
}
\value{
A data frame of tile coordinates and metadata.
}
\description{
Turns an arbitrary coordinate list into a standard wsiTools tile grid without
reading image pixels. Coordinates are level-0 slide pixels. By default, \code{x}
and \code{y} are interpreted as the top-left corner of each requested tile; use
\code{anchor = "center"} when the coordinates mark tile centers.
}
\examples{
slide <- wsiTools:::wsi_mock_slide(width = 1000, height = 800)
coords <- data.frame(x = c(0, 512), y = c(0, 256))
grid <- wsi_tile_grid_from_coords(slide, coords, tile_size = 256)
}
