% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask-annotations.R
\name{wsi_rois_to_mask}
\alias{wsi_rois_to_mask}
\alias{rois_to_mask}
\title{Rasterise ROI polygons into a mask}
\usage{
wsi_rois_to_mask(
  rois,
  width,
  height,
  scale = c(x = 1, y = 1),
  origin = c(x = 0, y = 0),
  label_by = c("index", "class", "roi_id", "name", "constant"),
  values = NULL,
  background = 0,
  overlap = c("last", "first", "error"),
  output = NULL,
  overwrite = FALSE
)

rois_to_mask(
  rois,
  width,
  height,
  scale = c(x = 1, y = 1),
  origin = c(x = 0, y = 0),
  label_by = c("index", "class", "roi_id", "name", "constant"),
  values = NULL,
  background = 0,
  overlap = c("last", "first", "error"),
  output = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{rois}{A \code{wsi_roi} object or a GeoJSON file path readable by
\code{\link[=read_geojson]{read_geojson()}}.}

\item{width, height}{Output mask width and height in pixels.}

\item{scale}{Numeric x/y size, in slide pixels, represented by one mask
pixel. Use values greater than 1 to create a lower-resolution mask.}

\item{origin}{Numeric x/y slide coordinate represented by the top-left corner
of the output mask.}

\item{label_by}{How ROI values are assigned: \code{"index"} gives one integer per
ROI, \code{"class"} gives one integer per unique class, \code{"roi_id"}/\code{"name"} give
one integer per unique ROI id/name, and \code{"constant"} burns all ROIs with the
same foreground value.}

\item{values}{Optional mask values. Use a length-one value, one value per
ROI, or a named vector/list keyed by the selected \code{label_by} values.}

\item{background}{Background mask value.}

\item{overlap}{How overlapping ROIs are handled. \code{"last"} lets later ROIs
overwrite earlier ROIs, \code{"first"} keeps the first value, and \code{"error"}
aborts when overlap is detected.}

\item{output}{Optional output file. \code{.rds} and \code{.csv} are written with base R;
image formats \code{.png}, \code{.tif}, \code{.tiff}, \code{.jpg}, and \code{.jpeg} require the
optional \code{magick} package and support 0-255 numeric labels.}

\item{overwrite}{Whether \code{output} may overwrite an existing file.}
}
\value{
A matrix with class \code{wsi_roi_mask}. Attributes include \code{origin},
\code{scale}, \code{labels}, \code{background}, and \code{output}.
}
\description{
Converts polygon or multipolygon ROI annotations into a small labelled mask.
This is the reverse of \code{\link[=wsi_mask_to_rois]{wsi_mask_to_rois()}} and is intended for annotation,
segmentation, and machine-learning masks. It rasterises ROI coordinates into
an output matrix without reading any WSI pixels.
}
\details{
Mask pixels are interpreted as pixel cells in slide coordinates. Pixel
\verb{(row = 1, col = 1)} has centre \code{origin + 0.5 * scale}; the same \code{origin}
and \code{scale} convention is used by \code{\link[=wsi_mask_to_rois]{wsi_mask_to_rois()}}.
}
\examples{
seed_mask <- matrix(0, nrow = 6, ncol = 6)
seed_mask[2:4, 2:4] <- 1
rois <- mask_to_rois(seed_mask, class_map = c("1" = "tumour"))
mask <- rois_to_mask(rois, width = 6, height = 6, label_by = "class")
}
