% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tissue-mask.R
\name{wsi_detect_tissue}
\alias{wsi_detect_tissue}
\title{Detect tissue/background in a low-resolution image}
\usage{
wsi_detect_tissue(
  image,
  method = c("simple", "otsu"),
  saturation_threshold = 0.05,
  brightness_threshold = 0.8,
  min_area = 1000,
  scale = c(1, 1),
  origin = c(0, 0)
)
}
\arguments{
\item{image}{RGB/RGBA array, raster, or magick image.}

\item{method}{Masking method. \code{"simple"} applies the supplied thresholds;
\code{"otsu"} uses Otsu thresholding on brightness and still requires minimum
saturation.}

\item{saturation_threshold}{Minimum HSV saturation for tissue.}

\item{brightness_threshold}{Maximum HSV value/brightness for tissue.}

\item{min_area}{Minimum connected tissue component size in thumbnail pixels.
Smaller components are removed before summaries are computed.}

\item{scale}{One or two positive numbers mapping mask pixels to source image
coordinates. Use slide-pixel scale for thumbnail-derived masks.}

\item{origin}{Source image x/y coordinate of the mask origin.}
}
\value{
A \code{wsi_tissue_mask} object containing the logical \code{mask},
\code{tissue_fraction}, \code{tissue_percentage}, \code{tissue_area},
\code{tissue_bounding_box}, and per-component \code{component_bboxes}.
}
\description{
Classifies pixels as tissue or background using HSV saturation and
brightness thresholds. The function is intended for thumbnails or small
regions, not for loading an entire whole-slide image into R memory.
}
\examples{
img <- array(1, dim = c(32, 32, 3))
img[10:20, 8:24, 1] <- 0.65
img[10:20, 8:24, 2] <- 0.25
img[10:20, 8:24, 3] <- 0.55
wsi_detect_tissue(img, min_area = 1)
}
