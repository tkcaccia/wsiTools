% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation-association.R
\name{wsi_annotate_seurat}
\alias{wsi_annotate_seurat}
\title{Add GeoJSON annotation categories to Seurat cell metadata}
\usage{
wsi_annotate_seurat(
  seurat,
  annotations,
  output = NULL,
  image_name = NULL,
  unassigned = "Unassigned",
  association_csv = NULL,
  engine = c("auto", "native", "r"),
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{seurat}{A Seurat object or the path to a \code{.rds} file containing one.}

\item{annotations}{A \code{wsi_roi} object or a GeoJSON file path.}

\item{output}{Optional path for the annotated Seurat \code{.rds} file.}

\item{image_name}{Optional Seurat spatial image name. By default the first
available image is used.}

\item{unassigned}{Category stored for cells outside all annotation polygons.}

\item{association_csv}{Optional path for a CSV containing cell coordinates
and their assigned annotation details.}

\item{engine}{Assignment engine passed to \code{\link[=wsi_associate_annotations]{wsi_associate_annotations()}}.}

\item{overwrite}{Whether existing \code{output} or \code{association_csv} files may be
replaced.}

\item{verbose}{Print a concise assignment summary.}
}
\value{
The annotated Seurat object. The following columns are added to
\code{meta.data}: \code{wsi_annotation}, \code{wsi_annotation_id}, and
\code{wsi_annotation_name}.
}
\description{
\code{wsi_annotate_seurat()} is a non-interactive workflow: it extracts the
full-resolution cell or spot coordinates stored in a Seurat object, assigns
each coordinate to an annotation polygon, and adds the result to the
object's \code{meta.data}. It does not launch the wsiTools viewer.
}
\details{
Polygon and MultiPolygon annotations are used for assignment. Point and line
annotations are ignored because they do not define an enclosed tissue area.
Cells outside every polygon are retained and receive \code{unassigned}.
}
\examples{
\dontrun{
annotated <- wsi_annotate_seurat(
  "cells.rds",
  "tissue_annotations.geojson",
  output = "cells_with_annotations.rds"
)
table(annotated$wsi_annotation)
}
}
