% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measurements.R
\name{measurement_report}
\alias{measurement_report}
\alias{wsi_measurement_report}
\title{Create a pathology measurement report}
\usage{
measurement_report(
  rois,
  cells = NULL,
  stains = NULL,
  pixel_size = NULL,
  image_origin = c(x = 0, y = 0),
  positive_threshold = NULL,
  ihc_intensity = TRUE,
  dab_threshold = positive_threshold \%||\% 0.1,
  output_dir = NULL,
  prefix = "wsi_measurements",
  overwrite = FALSE,
  max_pixels = 5e+06
)

wsi_measurement_report(
  rois,
  cells = NULL,
  stains = NULL,
  pixel_size = NULL,
  image_origin = c(x = 0, y = 0),
  positive_threshold = NULL,
  ihc_intensity = TRUE,
  dab_threshold = positive_threshold \%||\% 0.1,
  output_dir = NULL,
  prefix = "wsi_measurements",
  overwrite = FALSE,
  max_pixels = 5e+06
)
}
\arguments{
\item{rois}{A \code{wsi_roi} object.}

\item{cells}{Optional data frame or matrix with \code{x} and \code{y} cell coordinates.}

\item{stains}{Optional \code{wsi_ihc_channels} object returned by
\code{\link[=wsi_deconvolve_ihc]{wsi_deconvolve_ihc()}} or \code{\link[=wsi_deconvolve_multi_ihc]{wsi_deconvolve_multi_ihc()}}.}

\item{pixel_size}{Optional microns per pixel.}

\item{image_origin}{Level-0 x/y coordinate of the top-left pixel in \code{stains}.}

\item{positive_threshold}{Optional concentration threshold used for stain
positive-pixel fractions.}

\item{ihc_intensity}{Whether to add practical IHC ROI/class summaries with
DAB mean, DAB-positive area, hematoxylin density, and DAB/H ratio when
\code{stains} contains hematoxylin and HRP/DAB channels.}

\item{dab_threshold}{DAB threshold used for the IHC positive-area summary.
Defaults to \code{positive_threshold} when supplied, otherwise \code{0.1}.}

\item{output_dir}{Optional directory where report CSV files should be written.}

\item{prefix}{File prefix used when \code{output_dir} is supplied.}

\item{overwrite}{Whether to overwrite existing CSV files.}

\item{max_pixels}{Maximum ROI candidate pixels to mask for stain summaries.}
}
\value{
A \code{wsi_measurement_report} list containing data frames.
}
\description{
Combines ROI area summaries, per-class summaries, optional cell-density
measurements, nearest-neighbour distances, distance-to-boundary tables, and
optional hematoxylin/HRP-DAB stain intensity summaries. The function only
uses ROI coordinates, cell coordinates, and already-small/deconvolved stain
images supplied by the caller.
}
