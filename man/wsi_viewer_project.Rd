% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viewer-project.R
\name{wsi_viewer_project}
\alias{wsi_viewer_project}
\title{Open a multi-image pathology project viewer}
\usage{
wsi_viewer_project(
  images,
  output = NULL,
  open = interactive(),
  width = 1600,
  height = NULL,
  title = "wsiTools project viewer",
  overwrite = FALSE,
  roi_class_presets = wsi_roi_class_presets(),
  czi_sections = TRUE
)
}
\arguments{
\item{images}{Character vector of image paths, \code{wsi_slide} objects, or a
data frame/list of project image records.}

\item{output}{HTML file to write. When \code{NULL}, a temporary HTML file is used.}

\item{open}{Open the generated HTML file in a browser.}

\item{width}{Preview width used for openable files.
Larger values provide sharper zoomable previews, at the cost of a larger
self-contained HTML file. This is still a downsampled preview and does not
load the full source image into R. The generated project viewer uses the
OpenSeadragon shell so the View menu can split multiple project
images/sections into side-by-side panes even when full-resolution tiles are
not yet available.}

\item{height}{Optional preview height.}

\item{title}{Viewer title.}

\item{overwrite}{Overwrite \code{output} if it already exists.}

\item{roi_class_presets}{ROI class presets used by the annotation UI.}

\item{czi_sections}{For CZI files, show detected scenes/sections separately
in the Project panel. Set to \code{FALSE} to preview the whole CZI bounding box
as a single image when the backend supports it.}
}
\value{
Path to the generated HTML file, invisibly when opened.
}
\description{
\code{wsi_viewer_project()} creates a lightweight interactive viewer with a
Project section in the left annotation panel. It is intended for workflows
where a case contains multiple images or multi-scene microscopy files, such
as CZI. The function keeps large source files out of R memory and uses only
preview images in the browser. Format-specific high-resolution access remains
delegated to optional runtime backends.
Browser annotations are stored separately for each project image/section, so
ROIs drawn on one tissue section do not appear on another section.
}
\details{
CZI first visualization uses the direct native libCZI/libCZIAPI bridge when
available, but only after giving OpenSlide and libvips first refusal for CZI
variants they can already read. The older \code{aicspylibczi} bridge is used only
if the user explicitly sets \code{WSITOOLS_CZI_ALLOW_PYTHON=true}.
Bio-Formats command-line tools are used for metadata/conversion workflows.
For first visualization, wsiTools can also use a small optional Java helper
around Bio-Formats \code{ImageReader} region reads; it does not call \code{bfconvert}
automatically because batch conversion is too slow for interactive opening.
}
\examples{
\dontrun{
wsi_viewer_project(c("section_1.czi", "section_2.czi"))
}
}
