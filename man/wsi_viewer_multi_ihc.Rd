% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stain.R
\name{wsi_viewer_multi_ihc}
\alias{wsi_viewer_multi_ihc}
\title{View a multi-IHC slide with selectable stain channels}
\usage{
wsi_viewer_multi_ihc(
  slide,
  mode = c("tiles", "thumbnail"),
  channels = wsi_default_multi_ihc_channels(),
  ...
)
}
\arguments{
\item{slide}{A \code{wsi_slide} object.}

\item{mode}{Viewer mode passed to \code{\link[=wsi_viewer]{wsi_viewer()}}. \code{"tiles"} is recommended
for full-resolution inspection.}

\item{channels}{Stain channels created by \code{\link[=wsi_stain_channels]{wsi_stain_channels()}}. Up to
three channels can be deconvolved from RGB brightfield data at a time.}

\item{...}{Additional arguments passed to \code{\link[=wsi_viewer]{wsi_viewer()}}, such as \code{output},
\code{tile_dir}, \code{open}, \code{roi}, or \code{overwrite}.}
}
\value{
The HTML viewer path, invisibly.
}
\description{
Convenience wrapper around \code{\link[=wsi_viewer]{wsi_viewer()}} for brightfield multiplex IHC.
The \code{Stains} menu includes a master \code{mIHC} toggle plus \code{Original},
\verb{All stains}, and show-only buttons. The default channels are starting values
for hematoxylin, HRP/DAB, and a red chromogen; pass assay-specific vectors
with \code{\link[=wsi_stain_channels]{wsi_stain_channels()}} for real analysis.
}
\examples{
\dontrun{
slide <- wsi_open("multiplex_ihc.svs")
channels <- wsi_stain_channels(
  name = c("Hematoxylin", "HRP/DAB", "Fast Red"),
  vector = list(c(0.650, 0.704, 0.286), c(0.268, 0.570, 0.776),
                c(0.213, 0.851, 0.477)),
  colour = c("#4b3f99", "#8b5a2b", "#d73027")
)
html <- wsi_viewer_multi_ihc(slide, channels = channels, open = FALSE)
wsi_close(slide)
}
}
