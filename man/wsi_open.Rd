% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slide.R
\name{wsi_open}
\alias{wsi_open}
\title{Open a whole-slide image}
\usage{
wsi_open(
  path,
  backend = c("auto", "openslide", "vips", "omezarr", "imagemagick")
)
}
\arguments{
\item{path}{Path to a WSI or large image file.}

\item{backend}{Backend to use. \code{"auto"} detects OME-Zarr directories, then
prefers OpenSlide when available and falls back to libvips.}
}
\value{
An S3 object of class \code{wsi_slide}.
}
\description{
\code{wsi_open()} validates a file path, chooses a backend, and returns a
lightweight slide handle. It reads metadata and pyramid information only; it
does not load the full slide into R memory.
}
\examples{
\dontrun{
slide <- wsi_open("sample.svs")
wsi_info(slide)
wsi_close(slide)
}
}
