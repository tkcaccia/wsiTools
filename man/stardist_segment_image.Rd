% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmentation.R
\name{stardist_segment_image}
\alias{stardist_segment_image}
\alias{wsi_stardist_segment_image}
\title{Run StarDist on an image crop}
\usage{
stardist_segment_image(
  input,
  output,
  model = "2D_versatile_he",
  command = NULL,
  args = NULL,
  output_type = c("auto", "geojson", "csv", "mask"),
  prob_thresh = NULL,
  nms_thresh = NULL,
  overwrite = FALSE,
  run = TRUE
)

wsi_stardist_segment_image(
  input,
  output,
  model = "2D_versatile_he",
  command = NULL,
  args = NULL,
  output_type = c("auto", "geojson", "csv", "mask"),
  prob_thresh = NULL,
  nms_thresh = NULL,
  overwrite = FALSE,
  run = TRUE
)
}
\arguments{
\item{input}{Input crop image path.}

\item{output}{Expected StarDist output path.}

\item{model}{StarDist model identifier passed into \code{{model}} placeholders.}

\item{command}{External command. If omitted, uses
\code{WSITOOLS_STARDIST_COMMAND} or \code{stardist-predict2d} when available.}

\item{args}{Command arguments. Placeholders \code{{input}}, \code{{output}}, \code{{model}},
\code{{prob_thresh}}, and \code{{nms_thresh}} are substituted safely before calling
\code{\link[=system2]{system2()}}.}

\item{output_type}{Output type for \code{\link[=import_segmentation]{import_segmentation()}}.}

\item{prob_thresh, nms_thresh}{Optional StarDist thresholds.}

\item{overwrite}{Whether to overwrite \code{output}.}

\item{run}{If \code{FALSE}, return the command plan without running StarDist.}
}
\value{
A \code{wsi_stardist_result} object.
}
\description{
Runs an optional external StarDist command on a crop image and imports the
result when it is written as GeoJSON, CSV/TSV centroids, or an image mask.
wsiTools does not depend on StarDist; pass \code{command} and \code{args} for the
Python/CLI environment available on your machine.
}
