% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/open-viewer.R
\name{wsi_viewer_native}
\alias{wsi_viewer_native}
\title{Launch the native Rust/WGPU viewer from R}
\usage{
wsi_viewer_native(
  input,
  ...,
  backend = c("auto", "openslide", "vips", "native_czi", "bioformats", "omezarr",
    "imagemagick"),
  app_path = NULL,
  wait = interactive(),
  native_args = character()
)
}
\arguments{
\item{input}{An image path or existing \code{wsi_slide} object.}

\item{...}{Additional arguments passed to \code{\link[=wsi_viewer_live]{wsi_viewer_live()}}.}

\item{backend}{Backend passed to \code{\link[=wsi_open]{wsi_open()}} when \code{input} is a path.}

\item{app_path}{Path to the \code{wsitools-desktop} executable. \code{NULL} discovers
the installed desktop application.}

\item{wait}{Whether to keep servicing the live R bridge until interrupted.
This defaults to \code{TRUE} in interactive R and is normally what users want.}

\item{native_args}{Extra command-line arguments for the desktop executable.}
}
\value{
Invisibly, a \code{wsi_viewer_session}.
}
\description{
\code{wsi_viewer_native()} starts the same live R bridge used by
\code{\link[=wsi_viewer_live]{wsi_viewer_live()}} and opens its tile and state endpoints in the native
Rust/WGPU desktop renderer. R remains the owner of the slide, annotations,
spatial object, and analyses; the native application never loads the whole
slide into memory.
}
\details{
The function requires a wsiTools Desktop build with the \code{native-wgpu}
feature. The desktop launcher can be supplied explicitly through
\code{app_path}, or discovered from \code{WSITOOLS_DESKTOP_APP}, the macOS
Applications folder, or \code{PATH}.
}
\examples{
\dontrun{
wsi_viewer_native("sample.svs", dynamic_tiles = TRUE)
}
}
