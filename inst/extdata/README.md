# Example data

This directory is reserved for small package example files.

Whole-slide images are intentionally not bundled with the package because real
WSI files are usually too large for source distributions. Tests use mock slide
objects and tiny GeoJSON fixtures instead.
