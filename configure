#!/bin/sh

echo "checking for openslide-show-properties... `command -v openslide-show-properties || echo no`"
echo "checking for openslide-write-png... `command -v openslide-write-png || echo no`"
echo "checking for vips... `command -v vips || echo no`"
echo "checking for vipsheader... `command -v vipsheader || echo no`"

cat <<'EOF'
wsiTools can be installed without OpenSlide or libvips. Functions that need a
missing backend will report an informative error at runtime.
EOF

exit 0
